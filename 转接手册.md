# 转接手册 · 练琴搭子 PickBuddy（新电脑 · 新 AI 接手指南）

> 版本：v1.0（2026-08-30）｜ 适用代码版本：**v0.17.2**（commit `65b6ef5`，远程 `github.com/mengchiren/guitar-learning-assistant`，私有）
> 用途：把本工程从 `F:\电吉他学习` 搬到另一台电脑，让**新 AI（没有本次会话记忆）**接手时先看本文，再按步骤补齐环境、验证、开工。
>
> **文档读取顺序（新 AI 必读）**：本手册（环境与接手指南）→ `README.md`（功能/技术栈/测试/部署全景）→ `HANDOFF.md`（§2 文件地图、§3 完成清单、§6 踩坑 48 条、§7 与用户协作文注）→ `需求文档.md`（**唯一权威需求**，当前 v0.17.2；注意 HANDOFF §1 里「需求文档 v0.6.4」字样已过时）→ `验收指南.md`（手机验收清单）。

---

## 0. 这份手册解决什么问题

1. **复制工程**：哪些文件走 git、哪些被 `.gitignore` 挡住必须单独拷（音频/视频/壁纸等大文件）；
2. **工具盘点**：工程实际用到哪些工具、本机实测版本（第 3 节表格），新电脑需要装什么、**不需要**装什么；
3. **补齐环境**：缺失的环境件已补齐为仓库文件——`spike/requirements.txt`（Python 依赖锁，原缺失）、`env-check.sh`（一键自检脚本，新增）；
4. **验证标准**：跑一遍 `env-check.sh` + 构建 + 测试，全部通过即环境就绪。

---

## 1. 项目是什么（30 秒版）

「练琴搭子」是一个**纯前端为主的个人电吉他学习 PWA**（Vue 3 + Vite + Pinia + vite-plugin-pwa），已部署到 Cloudflare Pages（`https://guitar-learning-assistant.pages.dev`）。核心：上传歌曲 → 本地音频分析（BPM/调性/套路，Web Worker 后台跑）→ 按用户设备（Ibanez GRX40-LGY + JOYO Jam Buddy 2）给音色设置建议；另有练琴打卡/计划/曲谱/录音回听/AI 答疑（CF Pages Functions 代理）/云同步/主题壁纸等功能。产品策略：**种子库人工数据优先 → AI 分析作参考并标置信度 → 人工纠错兜底**。用户是零基础初学者，所有建议要「大白话 + 实物面板图」。

---

## 2. 复制工程到新电脑

### 2.1 三种方式对比

| 方式 | 做法 | 优点 | 缺点 | 适用 |
|---|---|---|---|---|
| **A. 整盘拷贝（推荐）** | 把整个 `F:\电吉他学习` 拷到新电脑同路径 | 连 `.git` 历史、`app/node_modules`、`spike/.venv`、大文件全带上，**拷完即可用** | 总大小约 **32.7 GB**（其中视频教程 30 GB） | 用户换机 |
| B. git clone | 新电脑 `git clone` 私有仓库 | 干净、只含可提交文件 | 仓库**私有**，需 GitHub 认证；且 git 不含大文件；node_modules/venv 要重建 | 新 AI 拿到授权后 |
| C. 只拷「仓库文件 + 大文件」 | `git` 内容打包 + 手动拷第 2.2 节清单 | 可控 | 容易漏 | 备选 |

**推荐组合**：A 或 B 都行；如果拷 A，新电脑上仍建议跑一遍 `env-check.sh` 确认；如果拷 B（clone 到新机器），按第 4 节搭环境。

### 2.2 被 `.gitignore` 挡住、必须单独拷贝的目录（git 中不存在）

| 目录 | 本机大小 | 用途 | 换机建议 |
|---|---|---|---|
| `视频教程/` | ~30 GB | 成田课程视频（版权） | **必须拷**（学习资料） |
| `歌曲文件/` | ~341 MB | 用户歌曲音频（已按「歌手 - 歌名」重命名） | **必须拷**（上传分析用） |
| `spike/songs/` | ~155 MB | 真实歌曲对拍音频（**仅本地跑，不进 CI**） | 需要跑真实对拍就拷，文件名必须与 `spike/test_frontend_analyze.mjs` 一致 |
| `壁纸文件/` | ~128 MB | Wallpaper Engine 壁纸包（`.mpkg`） | 可选 |
| `GP文件/` | 视内容 | 用户曲谱样例（当前以课件 PDF 为主） | 可选 |
| `gui-test-screenshots/` | ~672 MB | 验收/UI 截图留档 + CDP 截图脚本 | 可选（脚本可复用） |
| `.tmp-ref/` | ~323 MB | 设备实物照片/临时 Chrome profile | 可选 |
| `app/node_modules/` | ~177 MB | npm 依赖 | **不必拷**，`npm ci` 重建 |
| `spike/.venv/` | ~544 MB | Python 3.13 venv | **不必拷**，按 4.4 重建（`.venv` 绑本机路径） |
| `.zcode/` | 小 | 旧 AI 会话文件 | **不要拷**（与本工程无关） |

> 提示：git 只保证「已提交文件」；`dist/`、`dev-dist/`、`gui-test-screenshots/` 等本就被忽略，不用管。
> 拷录大目录建议用移动硬盘/局域网，视频 30 GB 走 U 盘耗时很长。

---

## 3. 工具清单（本机实测基线 vs 新电脑要求）

| 用途 | 工具 | 本机实测版本 | 新电脑最低要求 | 说明 |
|---|---|---|---|---|
| 系统 | Windows | 11（build 26100） | Windows 10 或 11 x64 | 无硬性要求 |
| 命令行 | Git for Windows（含 **Git Bash**） | 2.53.0.windows.3 | 任意近期版本 | **项目开发全程用 Git Bash**（HANDOFF 注）；PowerShell 也可，但中文输出需 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` |
| 运行时 | Node.js | v24.15.0 | **≥ 20.19**（Vite 8 硬性要求；CI 用 22） | 建议 22 LTS |
| 包管理 | npm | 11.12.1 | 随 Node | 用 **npm**（有 package-lock.json），**不用 pnpm/yarn**；镜像已配在 `app/.npmrc`（npmmirror，勿删） |
| Python | Python | 3.13.13 | **3.13.x**（64 位） | 仅 spike 用；3.13 移除 stdlib audioop/aifc/chunk/sunau，由 `audioop-lts` 等四个兼容包补齐（已进锁文件） |
| ffmpeg | imageio-ffmpeg（venv 内嵌） | 0.6.0（ffmpeg-win-x86_64-v7.1.exe） | **不需要装系统 ffmpeg** | 路径获取：`spike/.venv/Scripts/python.exe -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"` |
| 浏览器 | Chrome/Edge | Chrome（`C:\Program Files\Google\Chrome\...`） | 可选 | 仅 headless 截图验证用（`gui-test-screenshots/*.mjs`，CDP + `--remote-debugging-port`） |
| 手机验收 | 手机浏览器 | 用户安卓机 | — | 上线站点验收用，不依赖本机 |

**不需要安装**（重要）：Cloudflare Wrangler（部署走 CF Pages 后台 + GitHub push，本机零配置）、GitHub CLI、pnpm、任何音频分析库（引擎是自研纯 JS）。
> 本机全局 npm 里的 `dsh / codex / ark-cli / bailian-cli` 是 AI 宿主工具，**与工程无关**，别装也别引用。

---

## 4. 新电脑环境搭建（一次做完，共 6 步）

### 4.1 安装基础工具

```powershell
# 管理员 PowerShell（winget 一键；不想用 winget 就去官网下安装包）
winget install Git.Git
winget install OpenJS.NodeJS.LTS        # 或手动装 22 LTS：https://nodejs.org
winget install Python.Python.3.13       # 勾选 Add python.exe to PATH
```

安装后重开终端验证：`git --version`、`node -v`（≥v20.19）、`python --version`（3.13.x）。

### 4.2 获取代码

```bash
# 方式 B（推荐给新 AI）：私有仓库，需要 GitHub 凭据（HTTPS PAT 或已登录的 Windows 凭据管理器）
git clone https://github.com/mengchiren/guitar-learning-assistant.git
cd guitar-learning-assistant

# 方式 A：整盘拷贝则跳过本步，确认目录里 .git 存在即可
```

Git 身份与认证（推送需要）：

```bash
git config --global user.name  "mengchiren"
git config --global user.email "2112595291@qq.com"
# HTTPS 推送准备：Git Credential Manager 登录，或生成 PAT 后 `git credential-manager get` 校验
```

### 4.3 前端依赖

```bash
cd app
npm ci          # 严格按 package-lock.json 安装；镜像源在 .npmrc（npmmirror），慢/失败见 FAQ
cd ..
```

### 4.4 spike 虚拟环境（Python）

```bash
python -m venv spike/.venv                      # 若 4.1 装的是 3.13，此 venv 即 3.13
spike/.venv/Scripts/python.exe -m pip install --upgrade pip
spike/.venv/Scripts/python.exe -m pip install -r spike/requirements.txt
```

验证（应能打印出 ffmpeg 可执行文件路径）：

```bash
spike/.venv/Scripts/python.exe -c "import librosa, imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"
```

### 4.5 云端数据令牌（不是环境问题，但新电脑要用）

`ASK_TOKEN`（AI 答疑防刷）与 `SYNC_TOKEN`（云同步防刷）**只存在于浏览器 localStorage 与 CF 环境变量**：
- CF 侧：见第 7 节（Cloudflare 控制台配置，与新电脑无关，一次配好全端生效）；
- 浏览器侧：新电脑浏览器第一次用 AI 答疑/云同步时，在应用内输入令牌（`验收指南.md` M 节、0.6 节有步骤）；**换机后本地令牌不会迁移**，需要重新输入（令牌值问用户，或从旧电脑浏览器/CF 控制台拿）。

### 4.6 自检

```bash
bash env-check.sh     # 全绿 = 环境就绪（见第 5 节）
```

---

## 5. 环境自检脚本 `env-check.sh`

仓库根目录新增的一键自检（只读，不改文件），检查 6 组：

| 组 | 检查项 | 失败时怎么修 |
|---|---|---|
| 1 | git 存在、最近提交、工作区干净、origin | 装 Git；有未提交改动先 `git status` 确认 |
| 2 | node ≥ 20.19、npm 存在 | 重装 Node 22 LTS |
| 3 | 系统 python 3.13.x | 装 Python 3.13 |
| 4 | `spike/.venv` 存在、核心包（librosa/numpy/pymupdf/PIL/soundfile/imageio-ffmpeg）可 import、**ffmpeg 二进制就绪** | `python -m venv spike/.venv` + `pip install -r spike/requirements.txt` |
| 5 | `app/node_modules` 关键包齐全（vue/vite/pinia/vue-router/vite-plugin-pwa/pdfjs-dist/fake-indexeddb/@vue/server-renderer/@vue/compiler-sfc） | `cd app && npm ci` |
| 6 | 冒烟：`test_plan_engine.mjs`（25 项）+ `test_ear_training.mjs`（9 项） | 先解决上面依赖缺失 |

本机验证输出（2026-08-30）：`10 通过 / 0 失败 / 1 提醒`（提醒 = 系统 PATH 无 ffmpeg，**正常**，ffmpeg 走 venv 内嵌二进制）。

---

## 6. 常用命令速查

### 开发

```bash
cd app && npm run dev        # 默认 http://localhost:4173（可 -- --port 4174）
npm run build                # 生产构建到 app/dist；构建后 CI 会跑产物守卫
npm run preview              # 预览构建产物
```

### 测试（仓库根目录；改 `analyze.js`/`planEngine.js`/数据文件后必须重跑相关组）

```bash
# ffmpeg 路径（PowerShell 下先设 UTF-8 输出防中文乱码）：
#   FFMPEG=$(spike/.venv/Scripts/python.exe -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())")
node spike/test_frontend_analyze.mjs "$FFMPEG"  # 真实歌曲对拍 5/5（本地版权音频，CI 不跑）
node spike/test_frontend_synthetic.mjs          # 合成音频引擎测试（CI 跑）
node spike/test_plan_engine.mjs                 # 规则引擎 25 项
node spike/test_sheets.mjs                      # 曲谱/和弦数据 836 项
node spike/test_stores_smoke.mjs                # Store 冒烟 25 项
node spike/test_tone_guide.mjs                  # 套路规则+设备建议 53 项
node spike/test_sheet_view.mjs                  # 课件式谱面 SSR 渲染 19 项
node spike/test_wallpapers.mjs                  # 壁纸模块 26 项
node spike/test_rhythm_stability.mjs            # 录音节奏稳定度（CI 跑）
node spike/test_ear_training.mjs                # 听力训练逻辑（CI 跑）
node spike/test_sync.mjs                        # 云同步决策/清洗（CI 跑）
node spike/test_panels_render.mjs               # 面板组件渲染冒烟（CI 跑）
node spike/check_dist_assets.mjs app/dist       # dist 产物守卫
```

> `test_frontend_analyze.mjs` 的 5 首歌曲文件名与路径固定在 `spike/songs/`（见该文件 CASES 表），缺歌会直接失败——这是**本地占位数据**，不是环境问题。

### CI

`.github/workflows/ci.yml`：push/PR 自动跑三组并行（tests-logic / tests-engine-render / build-and-guard）。**全过才能合并**；真实歌曲对拍只在本地跑。

---

## 7. 云端配置（Cloudflare Pages）—— 新电脑不需要任何本地 CF 凭据

推送后 CF 自动部署到 `https://guitar-learning-assistant.pages.dev`，构建配置：

| 项 | 值 |
|---|---|
| 生产分支 | `main` |
| 构建目录 | `app` |
| 构建命令 | `npm run build` |
| 输出目录 | `dist` |
| NODE_VERSION | 22 |

**环境变量**（Cloudflare 控制台 → 站点 → Settings → Environment variables，改动后需 **Retry deployment**）：

| 变量 | 必配 | 用途 |
|---|---|---|
| `AI_KEY_DEEPSEEK` | ✅ | DeepSeek API Key（AI 答疑默认源） |
| `ASK_TOKEN` | ✅ | AI 答疑访问令牌（任意随机串；应用内 AI 答疑页首次使用时输入同一值；未配则接口 503） |
| `SYNC_TOKEN` | ✅ | 云同步访问令牌（同上，应用「我的 → 数据同步」输入） |
| `SYNC_KV` | ✅ | **KV 绑定**（不是普通环境变量）：Pages 控制台绑定一个 KV 命名空间，变量名 `SYNC_KV` |
| `AI_KEY_ARK` | 按需 | 火山方舟 Key |
| `AI_MODEL_ARK` | 按需 | 方舟接入点 id（`ep-xxx`） |
| `AI_KEY_QWEN` | 按需 | 通义千问 Key |
| `ALLOWED_ORIGINS` | 可选 | 逗号分隔追加域名白名单（默认含 `localhost:4173/5173` 与生产域名） |

SPA 深链接回退：`app/public/_redirects`（勿删）。

---

## 8. 换机数据迁移（重要：浏览器本地数据的取舍）

应用的「结构化数据」（打卡、歌单与分析结果、计划/课程进度、自录曲谱、聊天记录、设置偏好）有三条迁移路径：
1. **云同步**：「我的 → 数据同步」上传 → 新设备下载（推荐；录音与音频永不上云）；
2. **备份导出**：「我的」页导出 JSON → 拷到新设备 → 「我的」页恢复（录音除外）；
3. 什么都不做：本机 localStorage/IndexedDB 数据留在旧浏览器。

**换机会丢、需要重新上传/输入**（存本机浏览器，永不进 git/云端）：录音（IndexedDB）、原谱 PDF（IndexedDB）、壁纸（IndexedDB）、AI 答疑令牌、同步令牌、本机设置里的草稿/计时会话。

---

## 9. 工程红线（改了会出事，务必先看）

1. **版权内容不入库**：`歌曲文件/、视频教程/、壁纸文件/、GP文件/、spike/songs/` 已被 `.gitignore` 挡住，**不要 git add -f**；用户上传的 PDF/壁纸/录音只存本机 IndexedDB，不进 git、不进部署包、不上云同步。
2. **音频不出设备**：分析全在浏览器本地完成；唯一上行是 AI 答疑的提问文字（经 CF Functions 代理）与开启云同步后的结构化快照。
3. **改 `app/src/utils/analyze.js` 或 `planEngine.js` 必须重跑对应对拍**（test_frontend_analyze 或 test_plan_engine），并跑 CI 里对应组（坑 17 铁律）。
4. **新增可同步数据必须登记 `app/src/utils/syncKeys.js` 白名单**，并补 `spike/test_sync.mjs` 断言；`practice-loops`、`timer-session`、`practice-draft`、错误日志、`ear-best` 等本机态**禁止**入白名单。
5. **不改 `app/.npmrc`**（npmmirror 镜像），**不删 `app/public/_redirects`**（SPA 回退），**不动 `app/vite.config.js` 的 workbox globPatterns**（`**/*.{js,css,html,svg,woff2,webp,jpg,png}`，防 `.mjs`/`.wasm` 混出 precache，坑 45）。
6. **发版同步**：版本号三处（`app/package.json`、`需求文档.md` 版本头、HANDOFF 头部），文档更新（README/HANDOFF/需求文档修订记录）随功能一起提交——本项目惯例是「一次功能一个 commit + 完整回归」。
7. 详细 48 条踩坑记录在 `HANDOFF.md` §6；高频坑：坑 8（慢歌/低置信 BPM）、坑 17（改引擎跑对拍）、坑 25（webm 无时长）、坑 28（本机 GitHub push 间歇失败，用后台重试循环）、坑 39（帧内缓冲复用查跨帧引用）、坑 44/46（毫秒测量不可信 / decodeAudioData 会 detach 输入）。

---

## 10. 新 AI 接手检查清单（5 步）

1. **先读文档**：本手册 → `README.md` → `HANDOFF.md`（§2 文件地图 + §6 坑 + §7 协作）→ `需求文档.md`（唯一权威，功能争议以它为准；改需求先改它）→ `验收指南.md`。
2. **跑自检**：`bash env-check.sh`；全绿后 `cd app && npm run dev`，浏览器打开 `http://localhost:4173` 过一遍首页/工具页/歌曲库。
3. **确认仓库状态**：`git status` 干净、`git log -1` 与本文版本一致（v0.17.2 / `65b6ef5`）；不干净先问用户旧改动去向。
4. **云端检查**：push 后看 GitHub Actions 三组全绿 + CF Pages 部署成功；`AI_KEY_DEEPSEEK`/`ASK_TOKEN`/`SYNC_TOKEN`/`SYNC_KV` 四个必配项在 CF 控制台存在。
5. **再开始干活**。与用户协作注意：用户是零基础、非技术、中文交流；先给结论再用大白话解释；涉及需求变更先改「需求文档」；别把 AI 分析结果当精确数据。

---

## 11. 本机验证记录（编写本文时实测）

| 项 | 结果 |
|---|---|
| 工具版本 | Node v24.15.0 / npm 11.12.1 / git 2.53.0.windows.3 / Python 3.13.13 / Windows 11 (26100) |
| 全部 CI 型测试（11 组，仓库根目录直跑） | 全过：合成音频 3/3、规则引擎 25/25、数据 836/836、Store 冒烟 25/25、toneGuide 53/53、谱面渲染 19/19、壁纸 26/26、节奏稳定度 5/5、听力训练 9/9、云同步 21/21、面板渲染 17/17 |
| `npm run build`（app） | ✅ 1.34s，precache 88 项 3004.75 KiB，dist 守卫口径一致 |
| `bash env-check.sh` | 10 通过 / 0 失败 / 1 提醒（PATH 无系统 ffmpeg，正常） |
| git | 工作区已提交（本次新增 3 个文件：本手册、`spike/requirements.txt`、`env-check.sh`，已 commit） |

---

## 12. FAQ

**Q：`npm ci` 慢 / 超时？**
A：`app/.npmrc` 已配 npmmirror；仍失败可 `npm config get registry` 确认，不要删除 .npmrc；公司代理/防火墙场景换 `npm ci --registry=https://registry.npmmirror.com` 并配 `NODE_TLS_REJECT_UNAUTHORIZED`（谨慎）。

**Q：`python` 命令报“未找到”或者弹 Windows 商店？**
A：装的是 Python 3.13 官方安装包且没勾 PATH；重装勾选 **Add python.exe to PATH**，或直接用 `py -3.13`/全路径。

**Q：pip 装 librosa 报 audioop 相关错误？**
A：Python 版本不是 3.13 或 32 位；必须 3.13.x 64 位（`audioop-lts`/`standard-aifc`/`standard-chunk`/`standard-sunau` 专为 3.13 提供兼容）。

**Q：本地测试/脚本中文路径乱码？**
A：PowerShell 里先 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`；Git Bash 默认 UTF-8 无此问题。

**Q：`git push` 失败（已停止对应 GitHub 地址/网络抖动）？**
A：坑 28：国内网络间歇失败，用后台重试循环（`try { git push; break } catch { sleep }` 循环数分钟）；确认用 HTTPS + 凭据管理器/PAT。

**Q：CI 全绿但本地 `test_frontend_analyze.mjs` 失败？**
A：正常——该测试需要 `spike/songs/` 里的 5 首真实版权歌曲（git 不在库），CI 不跑它；没有歌就跳过。

**Q：`npm run dev` 端口被占？**
A：`npm run dev -- --port 4174`。

**Q：新电脑要不要装 ffmpeg / wrangler / pnpm？**
A：都不需要。ffmpeg 走 venv 内嵌二进制（imageio-ffmpeg）；部署走 CF Pages 后台；项目只用 npm。
