# 练琴搭子 · PickBuddy · 需求文档

- **版本**：v0.6.4
- **日期**：2026-08-18
- **状态**：已通过需求访谈（3 轮）+ 需求评审修订
- **用途**：作为后续开发的依据；开发前如有技术方案变更，需同步更新本文档

**修订记录**

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.10.2 | 2026-08-24 | **图表悬停气泡（用户要求：像 DeepSeek 开放平台那样鼠标放柱状图上显示详细时长，需求明确直接实施）**：①首页「最近 7 天」柱状图 hover 显示 `08/22 · 30 分钟 / 未练琴`；②统计页「近 4 周趋势」hover 显示 `08/24 周 · 1 小时 5 分（4 天）`；③月度热力图格子 hover 显示 `08/24 · 30 分钟`（替换原生 title）；④**手机等价交互**：tap 柱子/格子切换显示气泡（再点隐藏）；⑤全局 `.chart-tip` 样式（CSS 变量驱动，深浅主题/毛玻璃自适应）；⑥顺手修复：桌面端热力图格子过大（`max-width: 500px`，手机不变）。验证：CDP 派发 mouseenter 逐图截图核对（7 天柱/4 周柱/热力格 + 深色主题均可）。回归全过 + 构建过。**待验收。** |
| v0.10.1 | 2026-08-24 | **桌面布局自适应（用户反馈：页间布局位置不一致、导航栏两边空；讨论确认 A1 顶栏全宽 + 1280px + 自动列数 + 顺带微调）**：①顶栏移出 .shell 全宽铺满，内部 `topbar-inner` 限宽 1280 与内容区对齐（B 站/Notion 式，导航项不再锁死在中线）；②内容区最大宽度 1120→1280；③**卡片自动列数**：工具页 `auto-fill minmax(250px)`（1280 下 4 列，7 张卡 4+3 孤卡减轻）、歌曲页 `minmax(320px)`（1280 下 3 列）、和弦库桌面 `minmax(120px)`（6~10 列，手机保持 4 列）；④首页 hero 卡三区统一（今日分钟/连续打卡/开始练习并入一张卡，去掉桌面独立连击卡）；⑤Streak-big 死样式清理。手机端零变化。回归 6 组全过 + 构建过。**待验收：桌面版布局 + 手机回归。** |
| v0.10.0 | 2026-08-24 | **界面风格落地：新默认主题「DeepSeek 极简」（用户从 v0.9.0 的 3 款风格稿中选型，拍板参考 DeepSeek Harness 界面）**：①`data/themes.js` 新增 dsh 主题（排名第一）——近白 #fbfbfd 大留白、白卡细边框大圆角 16px、克制深蓝 #3d6ff2、页顶淡蓝光晕（--bg-image 用径向渐变，无需图片素材、不进 precache）、页面标题色块改蓝色小圆点；②**深色模式**：settings 新增 `darkMode`（system/light/dark 三档，我的→外观主题卡内选择，默认跟随系统；仅 dsh 主题实现深色变量块，其他主题忽略），App.vue 按 matchMedia + 设置同步 `html.dark`，深色下毛玻璃变量换深色半透明；③主题默认值迁移：旧默认 classic 选择自动迁移到 dsh（仍可手动切回）；④删除临时风格预览页（StylePreviewView.vue + /style-preview 路由）。回归 6 组全过 + 构建通过。**待验收：新默认主题 + 深色模式 + v0.9.0 性能优化手机体验。** |
| v0.9.0 | 2026-08-24 | **切换卡顿优化（用户反馈「切换界面卡」，先四维分析后确认 A 方案）+ 界面风格选款**：**性能**——①tab 页面 KeepAlive 保活（首页/计划/工具/歌曲/我的零重建，切回即显；HomeView 定时器改 onActivated/onDeactivated）；②tab 互切保留滚动位置（不改回顶）；③5 个 tab chunk 空闲预加载；④移动端低分辨率主题背景图（yui 257KB→109KB webp、maid 64KB→32KB，<768px 媒体查询切换）；⑤毛玻璃移动端降级（blur 14→6px，低端安卓 GPU 优化）。**风格**——①问卷定方向（可爱二次元 + 多邻国/星穹铁道参考 + 浅色带深色可切换 + 8px 圆角 + 信息密度不变 + 适度动效 + 渐变背景 + 主色随主题）；②临时预览页 `/style-preview`（3 款整页效果稿：星河指挥台/冒险打卡站/甜品练习室，浅深色可切），**选款定稿后删除并落地为新主题体系**。回归：合成 3/3、规则 25/25、数据 767/767、冒烟 24/24、toneGuide 53/53、面板 13/13、构建通过（precache 86 项 1373KB，KeepAlive 生效经 DOM 同节点验证）。**待验收：手机切换顺滑度 + 风格选款。** |
| v0.8.0 | 2026-08-20 | **代码审查修复（GLM 5.3 全量审查 → 逐条核实 → A+B+C 三组全部实施）**：**A 数据可靠性**——①计时落盘：timer 会话（running/startedAt/accumulated）随变更落盘、启动恢复 tick、跨天/超 12h 脏会话自动作废；打卡草稿（结束计时后的标签/备注）进 practice.draft 落盘，进程被杀重开可继续保存；②录音保存错误处理：失败明确提示不假「已保存」、<1 秒不保存；meta+blob 写入/删除改单事务原子化（消除孤儿 blob）；③存储满全局横幅：storage.save 失败通知全局监听者，App 弹「存储空间不足」横幅；④备份恢复：先清空全部 gla:v1: key 再写备份（真覆盖语义）+ 立即刷新（去掉 1.2s 竞态窗口）。**B 契约/视觉**——⑤TEMPLATE_IDS 补「金属 Riff」→heavy，并新增套路名↔模板 id 一致性护栏（7 项入 CI）；**顺带修出隐藏 bug**：classifyRules 的「清音+合唱氛围」与 templates.js name「清音 + 合唱氛围」空格不一致，归类命中的歌详情页设备建议缺失，统一为权威名；⑥硬编码色收敛（顶栏/底栏/胶囊/弹层/气泡 9 处）+ 毛玻璃覆盖补全；⑦调音器自相关按窗长归一化（修高八度误判）+ 指针量程 ±50 音分；⑧PWA 预缓存主题素材（webp/jpg/png）+ 补 192/512 PNG 图标。**C 小修**——系统通知落地（到点+已授权时 new Notification，每天一条）、节拍器 resume await、分析引擎帧内缓冲复用（修掉一个自引用 bug，对拍 5/5 验证）、ToneAdvice 设备响应式、package.json 版本同步 0.7.3、ChatView 列表 key 修复。回归：对拍 5/5、合成 3/3、规则 25/25、数据 767/767、冒烟 24/24、toneGuide 53/53、面板 13/13、构建通过。**待验收。** |
| v0.7.3 | 2026-08-19 | **看板娘换官方动画画风版（用户验收反馈）**：呆唯主题看板娘由 Q 版厚涂贴纸风换成官方动画画风候选（京都动画 K-ON! 风格半身像：茶色短发+黄色发卡+深蓝水手服红领巾+红吉他，v0.7.2 备选档），抠图转 WebP 31KB。回归：冒烟 20/20、构建通过。 |
| v0.7.2 | 2026-08-19 | **看板娘迭代（用户反馈：①要加看板娘启用开关；②鲸鱼女仆用现成素材；③唯的看板娘风格不行要优化）**：①**看板娘开关**——settings 新增 `mascotEnabled`（persist key `mascot`，默认开），「我的 → 外观主题」新增「显示看板娘」开关，关闭后主题配置了看板娘也不显示；②**鲸鱼女仆看板娘换现成素材**——弃用 AI 生成图，改用 DSH 社区 deep-whale-day-night-theme 仓库自带的 Q 版透明背景 companion（`assets/deep-whale-day-companion-v1.webp`，420×434 RGBA，62KB，蓝发女仆装人鱼尾持书 Q 版），与主题同源风格统一；③**平泽唯看板娘重制**——AI 生成两个风格候选（官方动画画风 9.5 分 / Q 版厚涂贴纸风 9.8 分，视觉模型评分），选用 Q 版厚涂版（茶色双马尾+黄色发卡+深蓝水手服红领巾+红吉他+拨片，张嘴傻笑露虎牙，还原度 95%+），抠白底+羽化转 WebP 59KB；官方动画风候选保留在 `gui-test-screenshots/review-20260819/yui-candidates/` 备选。回归：6 组全过（冒烟 20/20、规则 25/25、数据 767/767、toneGuide 46/46、面板 13/13、合成 3/3）、构建通过。**待验收**：新看板娘形象 + 开关。 |
| v0.7.1 | 2026-08-19 | **主题迭代（用户反馈三连）**：①**看板娘更萌**——呆唯主题看板娘由人偶实拍图换成 **AI 生成的 Q 版 chibi 平泽唯**（茶色双马尾+黄色蝴蝶结发卡+深蓝水手服+红色吉他，透明背景贴纸风，抠图脚本 `spike/cutout.py` 白底 flood-fill 去除+羽化，WebP 压缩 325KB→49KB）；②**透明毛玻璃模式**——「我的 → 外观主题」新增开关：开启后卡片/输入框/顶栏/底栏半透明 + backdrop blur(14px)，背景插画更透出来（`html.glass` 覆盖主题变量，优先级高于 `[data-theme]`，classic/yui/maid 三套主题通用）；③**新增第三主题「鲸鱼女仆 · 深海茶会」**——参考 DSH 社区 dsh-maid-whale-webUI 的设计语言（柔雾蓝 #4a86e8 强调、水彩鲸鱼云团背景、大圆角 12px、Q 版蓝发女仆抱小鲸鱼看板娘），背景/装饰素材直接取自该开源仓库（BSD-3-Clause，个人使用），AI 生图 Q 版鲸鱼女仆看板娘（WebP 83KB）；④细节：body 遮罩浓度改变量 `--bg-veil`（maid 主题 0.9 保可读性）。回归：冒烟 20/20、规则 25/25、数据 767/767、toneGuide 46/46、面板 13/13、合成 3/3、构建通过。**待验收**：三主题切换 + 毛玻璃开关 + 新看板娘。 |
| v0.7.0 | 2026-08-19 | **外观主题系统（用户要求：外观有更多样式，参考 DeepSeek Harness 社区「鲸鱼娘」类萌系主题，指定以轻音少女平泽唯为形象的「呆唯风」；素材网上收集、个人学习用途不公开传播）**：①**主题架构**——新增 `data/themes.js` 主题清单（id/名称/说明/色板预览/素材），settings 新增 `themeId`（persist 自动落盘），App.vue watch 写入 `html[data-theme]`，CSS 变量换肤；全站 20+ 视图硬编码色收敛为语义变量（--accent-soft/--ok-soft/--warn-soft/--warn-border/--warn-text/--bar/--heat-1/--heat-2），ChordChart/FretboardMap 红色点改 CSS class 跟随主题；**新增主题只加数据 + style.css 变量块，功能代码零改动**；②**首套新主题「呆唯 · 轻音海洋」**——奶油暖粉底（#fff5ec）+ 珊瑚粉强调（#f08a94）+ 圆角 10px；桌面背景为平泽唯弹吉他插画（body 背景图 + 86% 底色遮罩保证可读性，手机同享）；右下角常驻**看板娘**（新组件 `Mascot.vue`，点她随机说鼓励语，仅配置了 mascot 素材的主题显示）；③「我的」页新增**「外观主题」卡**（色板圆点预览 + 点击即时切换 + 选中高亮，桌面 2 列）；④素材：`app/public/theme/`（yui-bg-desktop.jpg 背景 263KB、yui-mascot.png 看板娘），SW precache 自动纳入；⑤顺手修复：ProfileView「5 套→6 套」文案漏同步、DevicesView 箱模名小写（od→OD）。回归：冒烟 20/20、规则 25/25、数据 767/767、toneGuide 46/46、面板 13/13、合成 3/3、构建通过。**待验收**：「我的 → 外观主题」切换；换肤后刷新两三次避开 SW 缓存。 |
| v0.6.4 | 2026-08-18 | **设备建议补全（用户反馈：琴两个旋钮只建议了音色）**：①**吉他音量旋钮（VOLUME）补全**——GRX40 两个旋钮（VOLUME/TONE）现在都给建议：音量旋钮进入 keyParams（参数速览第 2 芯片、操作步骤第 2 步「琴：音量旋钮拧到…」、面板示意图音量旋钮红圈高亮），音色旋钮保留在「可先不动」；6 套套路音量参考值：清音 7~8、轻过载 8~9、失真节奏 8~9、金属 9~10、主音 8~9；②**箱体模拟名大小写规范化**——用户此前输入小写仅为打字方便，实物为标准写法，全部统一为 65 Black Nor/OD、J800 Lo/Hi、DualRect Red、5153 EL34/6L6（OD 大写）。回归：toneGuide 46 项（基准重写含音量旋钮）、面板渲染 12→13 项（+音量旋钮高亮断言），全过；构建通过。**用户确认本轮交付结束。** |
| v0.6.3 | 2026-08-18 | **设备建议修正（用户实物反馈，v0.6.2 两个真错误）**：①**通道双脚钉逻辑讲全**——JAM BUDDY 2 通道是 CHANNEL 脚钉（CLEAN/DRIVE）+ DRIVE MODE 脚钉（DRIVE 状态下 RHYTHM/LEAD）两步，原文案只写了"按下 Rhythm 节奏通道按钮"没说清要按哪个脚钉；新增 `devices.js` 的 `channelMap`（模板通道语义值 → 脚钉操作），步骤自动拆两步（如「CHANNEL 脚钉按到 DRIVE（失真）」+「DRIVE MODE 脚钉按到 RHYTHM（节奏）」），参数速览芯片显示「DRIVE → RHYTHM」/「CLEAN」，面板图上 CHANNEL 与 DRIVE MODE 两个脚钉联动高亮并显示值；②**箱体模拟名修正**——原模板用 Rock/Metal/Blues 等虚构名（用户实物找不到），按用户提供的实物 14 种名单更正（65 Black Nor/OD、65 Black Vib/OD、J800 Lo/OD、J800 Hi/OD、DualRect Red/OD、5153 EL34/OD、5153 6L6/OD，OD=过载推子版），6 套套路重新映射（清音→65 Black Nor、轻过载→65 Black Nor OD、失真节奏→J800 Lo、金属→DualRect Red、主音→J800 Hi OD），新增 `modelNotes` 中文对照（芬达黑脸/马歇尔 JCM800/梅萨双整流/EVH 5150III）；音箱入门页新增「14 种箱体模拟对照表」卡片、通道操作说明；DevicesView 箱模列表更正。回归测试 41→46 项（+channelMap 覆盖校验、14 种名单校验、模板箱模必须在实物名单、虚构名禁止），面板渲染 11→12 项（双脚钉值标签断言），均过。 |
| v0.6.2 | 2026-08-18 | **图形化设备配置说明（用户反馈：文字版设备建议对零基础仍难用，原始需求 §3.1.3 的「面板示意图 + 音箱入门图解」补课落地；用户确认方案 A：SVG 示意图+高亮，两个模块都做，音箱+吉他一起画）**：①`devices.js` 扩展面板布局数据（JAM BUDDY 2：10 旋钮坐标/量程/多功能按压说明 + 3 脚钉；GRX40：琴身轮廓 + 5 档拨杆 + 音量/音色旋钮）；②新增 SVG 组件 `AmpPanel.vue`（数值旋钮按量程画指针角度 7:30→12:00→4:30、循环选择旋钮高亮+值标签、脚钉 LED + 值标签、interactive 模式点按）与 `GuitarPanel.vue`（档位单点/区间高亮、旋钮说明）；③**设备建议卡（新手模式）集成面板图**——要动的旋钮红色高亮 + 显示目标值（歌曲详情页/套路库页共用，数据驱动，换设备只改数据）；④**新增「音箱入门」页 `/amp-guide`**（工具页入口）：面板全景点按图解（每个旋钮/脚钉点一下看说明，含 Bass/Save 这类多功能按压提示）+ 6 套音色套路对照表 + 外部教学资源提示；⑤新增 `amp` 图标、nav.js 工具卡片；⑥回归测试：toneGuide 14→41 项（面板数据与套路模板一致性校验：套路字段必须在面板有对应旋钮/脚钉、坐标在画布内、无重复），新增 `spike/test_panels_render.mjs`（11 项，Vue SSR 渲染面板组件验证高亮/指针/值标签输出），均入 CI。全量回归通过：对拍 5/5、合成 3/3、规则 25/25、数据 767/767、冒烟 20/20、toneGuide 41/41、面板渲染 11/11、构建通过。 |
| v0.6.1 | 2026-08-18 | **音色套路库扩充（用户要求：按现有歌曲扩充）**：盘点种子库 29 首——「失真节奏 Riff」占比 86%（25 首），明显混着两类音色：重型金属（KiLLKiSS 200/Ave Mujica 215/黒のバースデイ 199 七弦/ギターと孤独と蒼い惑星 199 十六分 riff，小调暗色、闷音下拨为主）与常规日摇快歌（ふわふわ時間/青春コンプレックス/U&I 等明亮快扫）。**新增第 6 套套路「金属 Riff」**（Rhythm 通道 + Metal 箱模 + Gain 7 + 低音 6/高音 5 收高频 + 混响最轻，针对 GRX40 + Jam Buddy 2）；4 首重型歌从「失真节奏 Riff」改标为「金属 Riff」；`classifyRules.js` 置顶新增金属规则（bpm≥195 且 rmsDb>-14），引擎自动分析 190+ 快歌即命中。对拍测试期望同步（KiLLKiSS → 金属 Riff），toneGuide 回归测试 14→18 项（+3 金属边界断言 +1 金属套路基准）。本地回归全过：对拍 5/5、合成 3/3、规则 25/25、数据 767/767、冒烟 20/20、toneGuide 18/18、构建通过。 |
| v0.6.0 | 2026-08-18 | **工程评审修复第二批（第 1 步验收通过后实施，方案已于 v0.5.0 确认）**：①**数据层重构**——`storage.js` 升级（schema 版本号 + 按 key 迁移注册表 + save 变更订阅，云同步接入点；老数据零迁移）；新增 Pinia 自动持久化插件 `plugins/persist.js`（store 声明 `persist` 配置后状态变更自动落盘，防抖 150ms，消灭「手动 save 漏调」；与旧版共用同一批 key 与格式）；ID 改 UUID（`utils/id.js`，crypto.randomUUID + 回退）；②**歌曲模型统一**——`allSongs` 归一化单一实体（种子/用户歌同构，bpm/key/template 直接是有效值），`effectiveSong` 拼装逻辑收进 store，消费方（歌曲库/详情/计划）不再各自拼装；③**导航配置化**——新增 `data/nav.js` 清单驱动路由/tab/工具页，加页面只加一条；④**套路规则数据化**——classify 规则抽到 `data/classifyRules.js`（条件 DSL + 置信度分母表），新增套路只加数据不改引擎；⑤**设备能力声明式化**——devices.js 增加 `params`/`keyParams` 能力描述（旋钮/通道/效果及 on/off 文案），toneGuide 按设备渲染、缺参数自动跳过，新增设备只加数据；⑥**JSDoc 渐进式类型**——引擎输出契约（AnalysisResult/Confidence）、存储 schema（Song/StorageMeta）、跨 store 传参（PlanItem/PlanOutput）先盖住。新增回归测试 `spike/test_tone_guide.mjs`（14 项：classify 边界点 + 设备建议输出与 v0.5.x 基准逐字符一致 + 缺参数降级），冒烟测试升级 20 项（含自动落盘断言），均入 CI。**本地回归全过**：真实对拍 5/5、合成 3/3、规则 25/25、数据 767/767、冒烟 20/20、toneGuide 14/14、构建通过。**用户可见变化：无**（所有重构行为等价，纯工程改进）。 |
| v0.5.1 | 2026-08-18 | **Bug 修复（用户验收发现：电脑端首页空白）**：根因是 v0.5.0 重构 `stores/practice.js` 时删了本地 `todayStr` 函数定义但 getter 调用点未同步改名，运行时抛 `ReferenceError`（首页用到的 todaySeconds/streakDays/last7Days 全炸，Vite 构建不查未定义引用所以没拦住）。修复：调用点改为共用 `utils/date.js` 的 `localDateStr`；**新增 `spike/test_stores_smoke.mjs` 冒烟测试（16 项，实例化全部 10 个 Pinia store 并访问所有 getter/增删改 action，纯 Node + localStorage 内存 shim）并加入 CI**，防止同类「构建能过、运行时炸」的回归；顺手把全工程 63 处不带 `.js` 扩展名的相对导入统一补齐（Vite 目录解析的 `./router` 改为 `./router/index.js`，`seedSongs.json` 加 `with { type: 'json' }`，Node 可直接测试 store 模块）。本地回归全过：真实对拍 5/5、合成 3/3、规则 25/25、数据 767/767、冒烟 16/16、构建通过。 |
| v0.5.0 | 2026-08-18 | **工程评审修复第一批（用户发起：架构/扩展性/安全/维护四维评审后确认方案）**：①**AI 答疑防盗刷**——`/api/ask` 新增访问令牌（CF 环境变量 `ASK_TOKEN`，应用内首次使用时填写、请求带 `X-Ask-Token` 头校验；Origin 白名单只能防浏览器跨站调用，挡不住脚本直连，令牌层补上）；②**数据备份/恢复**——「我的」页新增导出/恢复（结构化数据 JSON 文件，录音不含，已确认）；③**CI 自动化**——GitHub Actions push 自动跑规则引擎 25 项 + 数据 767 项 + 合成音频引擎测试（新脚本 `spike/test_frontend_synthetic.mjs`，纯 JS 合成节拍音，覆盖快/中/慢三档 BPM 与慢歌不加倍）+ 生产构建；④**重复代码收敛**——新增 `utils/date.js`（本地时区日期，原 5 份重复实现合一）、`utils/music.js`（音名/调性/置信度徽章常量）、`utils/audio.js`（解码管线统一，歌曲分析与录音对拍共用，原两套实现合一）；⑤**分析引擎进 Web Worker**——`workers/analyze.worker.js` + `utils/analyzeWorker.js`，大文件分析不再卡 UI（Worker 不可用时自动回退主线程直调）；⑥版本号统一（package.json 0.1.0→0.5.0）。已确认决策：云同步（Supabase）暂缓（存储层留变更订阅口，第 2 步实施）、录音不纳入备份、类型方案选 JSDoc 渐进式（第 2 步）、按 IP 限流因 pages.dev 免费域名暂不可配（换自定义域名后补）。第 2 步（数据层重构/导航配置化/套路设备声明式化）待用户验收第 1 步后开工。 |
| v0.1 | 2026-08-14 | 初稿：3 轮需求访谈结论 |
| v0.2 | 2026-08-14 | 按需求评审修订：①音色建议改为「套路模板库 + 歌曲归类」方案（不再承诺逐歌 AI 生成参数）；②分析管线改为「AI 分析为主 + 种子曲库校准」（放弃第三方曲谱库依赖）；③新增关键用户流程与验收标准（§3.8）；④学习计划明确为规则引擎并定义采集信号；⑤离线范围收窄、账号简化；⑥成本评估修正开源工具描述；⑦路线图新增 M0 可行性验证（当前阶段） |
| v0.2.1 | 2026-08-15 | 用户确认：种子曲库自建路线、录音本地存储（§3.1.5/§5）；新增音频格式约束（酷狗 kgg 等加密格式无法分析，见 §3.1.1）；§2.2/§2.3 补充小组件与微信推送方案对比；M0 spike 完成 1 首真实歌曲初测（《NO, Thank You!》） |
| v0.2.2 | 2026-08-15 | 用户确认：Capacitor 打包方案（M4 实施）、微信推送用「推送加」（§2.2/§2.3）；M0 真实歌曲对拍完成：BPM 用 tempogram 峰值法可收敛到 ±5% 以内；调性识别在强力弦摇滚上不可靠（《NO, Thank You!》误判 E 小调/实际 A 小调；《空の箱》命中 F# 小调平行调）；和弦识别仅粗略。产品定位确定为「AI 参考 + 种子曲库校准 + 人工纠错」（§3.1.3/§7） |
| v0.2.3 | 2026-08-15 | M0 完成（5 首真实歌曲对拍）：BPM 修正规则简化为「半速加倍」，误差 ≤1.5%；调性 4/5 命中（含平行调）；套路归类 4/5 合理；和弦识别确认薄弱。结论：免费工具链可承担 BPM/调性参考/套路归类，和弦需种子曲库或付费 API；M1 无技术依赖可开工，付费 API 决策顺延至 M2 前 |
| v0.2.4 | 2026-08-15 | M1 首版开发完成（app/ 目录，Vue3 + Vite + PWA）：设备档案、音色套路库（5 套）、节拍器、调音器、练琴计时打卡、补卡、提醒设置、种子歌曲库（6 首）；本地存储 + 存储抽象层（为云同步预留）。浏览器实测通过，修复 2 个 bug（UTC 日期差一天、计时器响应式依赖） |
| v0.2.5 | 2026-08-15 | M1 视觉改版（按用户反馈）：简约瑞士军刀风——米白浅色底、瑞士红强调色、白卡细线、留白充足、线性 SVG 图标（新增 Icon 组件）；二级页面增加顶部返回按钮，底部导航仅四大主页显示 |
| v0.2.6 | 2026-08-15 | 桌面/移动端分离布局（按用户反馈，参考 B 站桌面版）：≥768px 桌面布局——顶部导航（品牌 + 四大导航 + 节拍器/调音器快捷入口 + 二级页返回键）、首页仪表盘多栏（主区今日时长/开始练习/7 天图 + 右栏连续天数/练习包/提醒）、工具页 3 列、歌曲库 2 列、设备 2 列、套路库 2 列、我的页 3 列、表单与工具类页面居中限宽 680px；<768px 保持原手机布局不变。新增 useMediaQuery 响应式工具；浏览器桌面/手机双视口实测通过 |
| v0.2.7 | 2026-08-15 | 体验修复（用户反馈「开始练习后无法使用其他功能」）：练习计时与节拍器提升为全局 store——切换页面不停表/不停声；外壳新增「练习中 mm:ss」胶囊（桌面在顶栏内、手机底部悬浮），点击一键回练习页；练习页内嵌迷你节拍器卡（BPM + 开始/停止 + 打拍定速），与节拍器页共享同一实例；浏览器实测通过 |
| v0.2.8 | 2026-08-15 | 按用户反馈移除练习页内嵌的迷你节拍器卡（用户不需要节拍器出现在计时界面）；保留计时/节拍器全局化与「练习中」胶囊 |
| v0.2.9 | 2026-08-15 | M2 歌曲分析（第一批，**纯前端架构，用户已确认「分析在浏览器本地跑」**，音频不出设备）：新增纯函数分析引擎 `analyze.js`（BPM 自相关+半速加倍 / RMS/谱质心 / K-S 调性 Top3 / 三和弦模板 / 启发式套路归类 / 置信度徽章；Node 对拍 5 首真实歌曲 5/5 全项通过，BPM 误差 ≤3%，调性 Top3 内命中 5/5）；歌曲库升级（搜索、卡片进详情、添加歌曲）；歌曲详情页（设备设置建议按当前设备标注、「用此 BPM 开节拍器」联动、人工纠错、种子歌只读）；添加歌曲页（上传本地分析 + 手动录入兜底，kgg 等加密格式拒绝并引导换源）；用户歌单存 localStorage（key: songs，纠错值优先展示） |
| v0.3.0 | 2026-08-15 | 按用户反馈「设备设置建议对新手太复杂」：新增全局显示偏好（「我的」页开关，**默认新手模式**）——新手模式为「参数速览（档位/通道/箱模/Gain 四个芯片）+ 大白话分步操作流程 + 其余参数标注可先不动」；完整模式保持原 9 项参数表。歌曲详情页与音色套路库共用 ToneAdvice 组件，一处切换全局生效（localStorage key: display-mode） |
| v0.3.1 | 2026-08-15 | 上传限制放宽（用户反馈无损文件被 30MB 门槛拦截）：文件上限 30MB→100MB；解码改为 OfflineAudioContext 直接渲染 22050Hz 单声道（峰值内存省约 4 倍）；新增解码后按实际 PCM 数据量（>300MB）的内存护栏；34.5MB 无损 flac 实测分析通过 |
| v0.3.2 | 2026-08-15 | 慢歌测速修复（用户反馈春日影分析置信度低）：①半速加倍改为**节拍强度比较法**——测值 <100 时比较 lag 与 lag/2 自相关强度（阈值 0.98；实测快歌半速测值两者比值 0.995~1.000，真实慢歌 <0.973），快歌才加倍、真实慢歌保持原值，5 首基准回归 5/5 通过；②慢歌 BPM 置信度封顶「中」并提示对照节拍器/种子库；③春日影加入种子曲库（权威 97 BPM / B 大调 / 清音伴奏，社区 MIDI 多源交叉验证）。已知局限：春日影类「1.5 倍谐波」测速（实测 64.6 vs 真 97）无法自动纠正（修正会误伤快歌），靠种子库权威值 + 人工纠错兜底 |
| v0.3.3 | 2026-08-16 | **部署上线**：Cloudflare Pages + GitHub 自动部署（私有仓库 `mengchiren/guitar-learning-assistant`）；构建配置：根目录 `app`、`npm run build`、输出 `dist`、`NODE_VERSION=22`；SPA 深链接回退 `_redirects`；PWA 主题色修正为米白。站点 `https://guitar-learning-assistant.pages.dev` 已上线（首页/深链接/SW/manifest/JS 资源验证通过）。注意：pages.dev 免费域名国内访问可能不稳定，后续可选自定义域名 |
| v0.3.4 | 2026-08-16 | 项目定名「练琴搭子 · PickBuddy」（用户选定）：应用品牌名（桌面顶栏/页面标题/PWA manifest 的 name·short_name·description/「我的」页关于文案）、GitHub 仓库描述、新增根目录 README.md 同步更新 |
| v0.3.5 | 2026-08-16 | 用户线上验收反馈修复：①路由切换滚动位置不重置——前进进新页面回顶部、后退返回时恢复原位置（router scrollBehavior）；②节拍器圆点不跳动——全局 store 新增当前拍状态，声音排拍同一时刻驱动圆点高亮（切页不停声时视觉同步）；③提醒时间无法输入——time 输入框在部分浏览器弹不出选择器，改为「时/分」两个下拉框（分钟按 5 分钟步进）。另新增根目录《验收指南.md》（手把手手机验收清单 + 反馈模板 + 常见问题） |
| v0.3.6 | 2026-08-16 | **M3 学习计划（§3.3 主体 + §3.4 课程跟踪 + 统计报表）**：①底部导航新增「计划」标签（5 标签：首页/计划/工具/歌曲/我的）；②规则引擎练习包（`utils/planEngine.js` 纯函数 + `spike/test_plan_engine.mjs` 对拍 25/25）：三档 10/30/60 分钟弹性练习包，每项含练法/达标标准/「为什么」解释；动态调整——上周 ≤2 天减半、连续 3 周达标 +10%、上周练得少默认展示 10 分钟档；③基本功清单 5 项（爬格子/开放和弦转换/节奏扫弦/闷音下拨/强力和弦移动，`data/fundamentals.js`），达标标记可在计划页勾选、统计页点击撤销；④歌曲练习状态「练习中/已掌握」（歌曲详情页 + 计划页），已掌握自动推荐同套路下一首（按套路模板匹配）；⑤统计报表页：本周概览、近 4 周趋势、月度热力图、基本功清单状态、歌曲掌握列表；⑥课程进度页（`data/courseCatalog.js` 由 `spike/gen_course_catalog.py` 扫描本地视频教程文件名生成，共 194 课：基础 73/中级 81/核心 40）：三套课切换、逐课勾选已学、自动记录学到第几课、进度百分比；⑦首页「今日练习包」卡改为读引擎真实输出（替代硬编码）。简化决策：歌曲「段落级」拆解简化为整首掌握标记（种子库暂无段落数据）；成就徽章（P2）留待后续 |
| v0.3.7 | 2026-08-16 | **计划项详情页 + 曲谱（按用户四选：种子谱人工整理/和弦谱形式/基本功任务分解+图示/仅个人本地使用）**：①基本功详情页 `/plan-item/basic/:id`：任务分解分步（练什么/几组几遍）+ 图示（和弦转换→C/G/Am/F 指法图；扫弦→节奏型符号 ↓/↓↑；爬格子→指板图；闷音/强力和弦→五和弦指法图）+「用目标速度开节拍器」联动 + 达标切换按钮；②自绘 SVG 和弦指法图组件 `ChordChart.vue`（6 弦 5 品、空弦/不弹标记、横按起始品标注、无图占位），图库 `data/chords.js` 19 个和弦（开放/横按/强力和弦）；③歌曲详情页新增「曲谱（和弦谱）」区：分段（前奏/主歌/副歌）+ 和弦进行 + 节奏提示，谱中和弦自动渲染指法图；种子曲谱 `data/songSheets.js` 首批 2 首（NO, Thank You! 完整分段；空の箱 标注校准），其余显示「暂无曲谱」；④用户自录曲谱：录入表单（分段增删、段名/和弦/节奏/备注）存 localStorage（`gla:v1:user-sheets`），用户谱优先显示、可编辑删除；⑤计划页与首页练习包项目可点击进详情；⑥数据完整性校验 `spike/test_sheets.mjs`（曲谱引用歌存在/谱中和弦在图库有定义/图库数据合法，96 项断言）。合规：曲谱为人工整理学习笔记、仅本地存储、无分享导出（用户确认）；宁缺毋滥原则——没把握的歌不录入谱子 |
| v0.3.8 | 2026-08-16 | **爬格子指板图改竖版 + 和弦图库页（用户三选：图库页样式/工具页入口/扩到 40+）**：①`FretboardMap.vue` 改为竖版——琴头朝上，1 品在最上面往下递推 2/3/4 品，品数标在左侧、弦号标在顶部；②和弦图库扩到 **49 个**（`data/chords.js` 加 category 分组字段）：开放 9、七和弦 13（7/maj7/m7）、挂留与延伸 6（sus2/sus4/add9）、横按 10、强力和弦 8、转位低音 3（D/F#、C/G、G/B），全部标准按法；③新增「和弦图库」页 `/chords`：按 6 组展示全部指法图，点任意和弦弹层放大（大图 + 横按提示）；④工具页新增第 4 张卡片「和弦图库」（新增 chord 图标）；⑤校验脚本升级（数量 ≥40、分组合法、每组非空，共 212 项断言） |
| v0.4.0 | 2026-08-16 | **M4-1 录音回听 + 自动对拍（用户选定：双入口/录完自动对拍/四分类与打卡一致）**：①录音本地存储——新增 `utils/recordingsDb.js`（IndexedDB 封装：元数据与音频 blob 分两个 object store，列表页不载入音频；localStorage 5MB 上限放不下录音）；②录音面板 `components/RecordPanel.vue`（练习页 + 工具页「录音回听」双入口）：MediaRecorder 兼容选择 mimeType（webm/opus 优先、mp4 兜底，覆盖安卓 Chrome 与 iPhone Safari）、单次最长 10 分钟、录音中离开页面自动停止；③录完自动对拍——`utils/recordAnalyze.js` 复用 `analyze.js` 引擎（解码→混合单声道→只分析前 3 分钟→秒级出结果），结果标置信度（高/中/低），可手动修改（标注「手动」），测不出（太短/噪音）留空不阻塞保存；④回听页 `/recordings`：录音列表（日期/时长/分类/歌曲/BPM/备注）、行内播放（Blob URL）、两段式删除确认；⑤分类与打卡一致（歌曲/基本功/课程/自由练习），选「歌曲」可关联种子库/用户歌，为后续「按歌回顾录音」留口；⑥工具页新增第 5 张卡片「录音回听」（新增 mic 图标）。安全：音频全程不出设备、零服务端。已知环境限制：麦克风需 HTTPS 安全上下文（线上 OK，局域网 http 不行，与调音器一致）。**Bug 修复（用户反馈）**：MediaRecorder 录的 webm 文件头不带时长 → 播放进度条拖不动/总时长显示异常——保存前用 fix-webm-duration 补写 Duration 字段（mp4 无此问题；该包是纯 CJS，需加入 Vite optimizeDeps 预构建，否则 dev 下动态 import 静默失败） |
| v0.4.2 | 2026-08-16 | **曲谱大扩充 + 新歌入库（用户提供 23 首歌曲文件）**：①整理 `歌曲文件/` 目录——酷狗格式文件名按 ID3 元数据重命名为「歌手 - 歌名」（23 首），批量跑前端分析引擎得 BPM/调性/套路；②种子库 7→**29 首**（新增 Ave Mujica×4、MyGO!!!!!×6 新曲、結束バンド×6、トゲナシトゲアリ×3、放課後ティータイム×3；引擎分析值入库并标注「待人工校准」，社区谱源权威值优先）；③种子曲谱 2→**19 首**：5 个搜索代理并行检索曲谱信息，多源交叉验证录入（ふわふわ時間/カラカラ/星座になれたら/天使にふれたよ 为多源 high，春日影/忘れてやらない等 medium 标注校准状态，影色舞等单源自动检测标「请对照原曲校准」）；**宁缺毋滥**——10 首搜不到可靠和弦进行（KiLLKiSS/雑踏/迷星叫/タネビ/なにが悪い/爆ぜて咲く/声なき魚/誰にもなれない私だから/Deathly Loneliness Attacks/Singing!）不录谱；④和弦图库 49→**82 个**（补升/降号调三和弦 12、七和弦 15、挂留 4、强力和弦 2 的标准按法，横按标注起始品）；⑤校验升级到 **767 项断言**全过（每首新谱的和弦必须在图库有定义）；⑥谱内复杂色彩和弦（转位/maj9/减半七等）统一简化为基本和弦并在备注说明，遵守「学习笔记、标注来源与校准状态、仅本地」红线。 |
| v0.4.1 | 2026-08-16 | **M4-2 AI 答疑（用户选定：多平台可切换·先接 DeepSeek/聊天页+页面内入口）**：①**安全架构**：纯前端不能直连大模型（Key 暴露），新增 Cloudflare Pages Functions 代理 `app/functions/api/ask.js`（路由 `POST /api/ask`）——API Key 只存 Cloudflare 控制台环境变量（`AI_KEY_DEEPSEEK`/`AI_KEY_ARK`/`AI_KEY_QWEN`），不进 git、不下发浏览器；三平台都是 OpenAI 兼容格式一张表切换（DeepSeek api.deepseek.com；火山方舟 ark.cn-beijing.volces.com 的 ep-xxx 接入点；百炼 dashscope compatible-mode）；输入护栏（20 条内/单条 4000 字/角色白名单）+ max_tokens 1500；本地开发跨域调线上 Functions（CORS 头），生产同源；②聊天页 `/ask`（工具页第 6 张卡片「AI 答疑」，sparkle 图标）：气泡对话、模型切换 chips（DeepSeek/豆包/通义千问，选择持久化）、历史存本地（`gla:v1:ai-history`，20 条滚动窗口）、清空对话、Enter 发送；未配 Key 的平台给出明确提示；③**页面内入口带上下文**：歌曲详情页「问 AI 这首歌怎么练」、基本功详情页「问 AI 这个练习怎么练」——上下文（歌名/BPM/调性/套路/难度/练习状态/和弦 或 练习任务/达标标准/目标速度/达标状态 + 设备档案）经 sessionStorage 传入，提问时随系统提示词一起发给模型；④系统提示词固定为「练琴搭子」角色（零基础用户、大白话分步、结合学习数据回答）。成本：按 token 计费，单次答疑几分钱；用户需自备至少一个平台 API Key（DeepSeek 平台注册充值即可，百炼/方舟按需开通），Key 配置方式在验收指南中说明 |

---

## 1. 项目概述

### 1.1 背景与目标

用户是一名电吉他**零基础**初学者，因喜爱少女乐队题材动漫（轻音少女、孤独摇滚、MyGO!!!!!、Ave Mujica、哭泣少女乐队 GBC）开始学琴，同时也喜欢国内经典歌曲（光辉岁月、海阔天空等）。用户现有设备：**依班娜（Ibanez）GRX40-LGY 复古白电吉他** + **JOYO Jam Buddy 2 音箱**，并购买了成田电吉他课程视频。

用户面临的核心困难：不知道每首歌该把自己的琴和音箱调成什么样（尤其不太会用音箱）、不知道该先练什么怎么练、作为初学者难以坚持练琴。

因此需要一个贯穿「**练什么 → 设备怎么设置 → 怎么练 → 坚持练**」全流程的个人学习助手。

### 1.2 目标用户与使用场景

- **唯一用户**：本人（零基础，时间不固定，弹奏时间弹性）
- **使用场景**：
  - 电脑前：搜索/上传歌曲、查看分析结果与设置建议、查看学习计划与统计
  - 练琴时（手机在旁）：练琴计时打卡、查看音箱/吉他设置、用节拍器/调音器、AI 答疑

### 1.3 核心价值（一句话）

> 上传想练的歌，告诉我「我的琴和音箱该调成什么套路、先练哪段、跟着节拍器练到多快、这首歌适不适合现在的我」，并提醒和鼓励我每天坚持练琴。

---

## 2. 产品形态与技术约束

### 2.1 形态决策

| 事项 | 决策 |
|---|---|
| 产品形态 | **响应式网页（PWA）**：一套代码，电脑浏览器 + 安卓手机浏览器均可使用；手机端可「添加到主屏幕」当 App 用 |
| 跨端数据同步 | **必须**。曲库、打卡记录、学习进度、设备档案等所有数据云端存储、多端实时同步 |
| 手机系统 | 仅需重点适配 **Android**（iOS 不做硬性要求） |

### 2.2 桌面小组件的技术约束（重要）

用户希望有**多邻国式桌面小组件**（显示连续打卡天数等）。

**约束**：纯安卓 PWA 原生**不支持**桌面小组件。

**建议方案**（✅ 已确认，2026-08-15；Capacitor 打包在 M4 阶段实施）：
- 第一阶段：Web Push 系统通知 + 应用图标，先满足提醒需求
- 第二阶段（M4）：用 **Capacitor 把同一套网页代码打包成轻量安卓壳 App**——网页代码 100% 复用，安装后呈现为手机桌面图标，点开即全屏网页界面（无浏览器地址栏）；在此基础上实现桌面小组件（多邻国式卡片：连续打卡天数 + 今日练习状态，点击打开应用）与更可靠的常驻通知
- 说明：小组件本体是壳 App 内的一小块安卓原生代码（网页做不了原生小组件）；个人使用可侧载 APK 安装，无需应用商店上架

### 2.3 推送渠道

1. 安卓系统通知（Web Push，PWA 安装后可用）
2. 微信推送（**已确认使用「推送加」（v0.2.2）**：免费额度 200 条/天；Server酱免费档仅 5 条/天。二者均为 HTTP 接口 + 微信模板卡片推送，后续如需切换成本极低）
3. 应用内提醒（兜底）

### 2.4 预算偏好

用户要求「帮我评估」。**评估结论见第 6 章**：首版采用「免费 + 开源工具」组合，月成本可控制在 0~几元；是否需要付费能力（音频分析 API 等）由 M0 可行性验证的实测结果决定，上限每月几十元。

---

## 3. 功能需求

优先级说明：**P0** = 首版必须有；**P1** = 第二阶段；**P2** = 后续增强。

### 3.1 【P0】歌曲分析 + 设备设置建议（核心功能）

> **产品策略（v0.2 修订）**：逐歌人工标注音色参数不可规模化；从混音完成的音频里自动提取「音色特征」（增益大小、EQ 曲线、合唱深度）目前没有成熟技术，AI 直接生成旋钮建议容易「听起来像那么回事但实际不对」，对不会听音色的新手反而有害。因此音色建议采用**「音色套路模板库 + 歌曲归类」**方案：预先人工建立若干标准音色套路模板（参数经过人工校验），AI 只负责把歌曲归类到某个套路，参数从模板映射到用户的具体设备。模板可控、可解释、可反馈修正。

#### 3.1.1 输入方式

- **方式 A：按歌名搜索**——先查种子曲库和分析结果缓存，命中直接返回
- **方式 B：上传歌曲音频文件**——**主力路径**（覆盖动漫歌等曲谱库没有的歌曲）
- **格式约束（v0.2.1 新增）**：支持 mp3 / flac / wav / m4a 等通用格式；**酷狗 kgg、QQ音乐 mflac 等加密格式无法分析**，需以通用格式获取音频或改用歌名检索

#### 3.1.2 分析管线（v0.2 修订）

1. **主力：AI 音频分析**——上传音频 → 分析节奏（BPM/节拍）、调性、和弦、段落结构、风格归类
2. **辅助：种子曲库校准**——人工整理的歌曲数据（和弦/调性/难度/套路归属）用于校正 AI 结果，提供准确的和弦谱
3. **兜底：手动选套路**——分析失败或不可用时，用户可从套路模板列表中手动选择
4. 分析结果缓存；同一首歌再次查看免重复分析

#### 3.1.3 输出内容（五块）

1. **音色设置建议**：AI 将歌曲归类到音色套路模板（清音伴奏 / 清音+合唱氛围 / 轻过载节奏 / 失真节奏 Riff / 失真主音 Solo 等）→ 按当前设备档案映射为具体参数：
   - 吉他侧：拾音器 5 档选哪档、音量/音色旋钮位置
   - 音箱侧：通道（Clean/Rhythm/Lead）、箱头模拟型号、Gain、三段 EQ、MOD/Delay/Reverb 参数
   - 呈现方式：参数列表 + 设备面板示意图（旋钮位置高亮）+ 文字讲解 + 归类可信度标注
   - 用户可反馈「准 / 不准」并可微调存档（反馈回流用于修正模板）
2. **音箱使用入门**（重点，用户明确不会用音箱）：面板各旋钮/脚钉功能图解、音色套路对照表（清音/轻过载/失真节奏/失真主音/合唱氛围各自怎么调）
3. **和弦与调性**：歌曲调性、和弦进行、每个和弦的和弦图；**种子曲库数据优先于 AI 识别结果**；AI 识别标注置信度（M0 实测：摇滚歌曲调性/和弦识别不可靠，仅作参考），支持人工纠错
4. **练习步骤建议**：分段拆解、每段难点说明、配合节拍器的目标速度（支持递进：如 80% → 100% 原速）
5. **难度评级与适配度**：结合用户当前水平给出建议——「现在就能练 / 先练基本功 XX 再练 / 暂时别碰」

#### 3.1.4 音色套路模板库（本工程自建的数据资产）

- 模板结构：`{ 套路名, 适用曲风, 吉他档位, 音箱通道/箱模/EQ/效果参数, 说明 }`
- 首批模板：清音伴奏、清音+合唱氛围、轻过载节奏、失真节奏 Riff、失真主音 Solo
- 设备映射：模板存「通用参数」，按设备档案映射为具体型号的旋钮位置
- 数据来源：人工整理「歌曲 → 套路」映射（首批覆盖种子曲库 20~50 首歌）+ 用户反馈修正
- 示例（Jam Buddy 2）：J-Rock 动漫歌清音扫弦 → 「清音+合唱氛围」模板 → Clean 通道 + Clean 箱模 + 轻 Chorus + Hall 混响

#### 3.1.5 歌曲数据来源（v0.2 修订）

- **自建种子曲库（用户已确认，v0.2.1）**：人工整理用户歌单（附录 A）的和弦/调性/难度/套路归属——作为 AI 结果的校准基准；首批 20~50 首歌的歌单待与用户共同确认后录入
- **不依赖第三方曲谱库**：无稳定免费 API（Ultimate Guitar/Songsterr 均无公开 API）、动漫歌覆盖率低、爬虫脆弱且合规风险大；第三方数据仅作为人工录入时的参考
- **AI 音频分析是事实上的主力**：质量边界由 M0 验证（见第 7 章）

#### 3.1.6 设备档案（支持多设备）

- 可录入**多把吉他 / 多个音箱**，分析时选择「当前正在用的设备组合」
- 设备参数模板库：先内置 **Ibanez GRX40 + JOYO Jam Buddy 2**（参数见第 4 章），支持后续扩展新设备型号
- 音色建议引擎原理：歌曲归类结果 → 套路模板参数 → 映射到所选设备的面板参数

### 3.2 【P0】打卡与提醒

- **打卡流程**：进入练习 → 点开始计时 → 练完结束，自动生成记录；结束后可**手动补充**「练了什么」（歌曲 / 基本功 / 课程 + 备注）
- **打卡维度**：练习时长 + 练习内容标签（用于进度分析）
- **提醒**：每日练琴提醒（可配置提醒时间；考虑用户时间不固定，支持「今天还没练」的晚间兜底提醒）
- **连续打卡展示**：连续天数、月度练习日历
- **补卡机制**：允许补记（对时间不固定的用户友好）

### 3.3 【P0】学习计划

- **输入**：零基础水平 + 时间不固定 + 想练歌曲清单 + 课程资源
- **引擎定位（v0.2 修订）**：**规则引擎，不用 AI 生成计划**——采集明确的信号，按预定义规则生成练习包与调整建议，保证可控、可解释
- **采集信号清单**：每日打卡时长、练习内容标签、歌曲段落完成标记、基本功清单达标标记
- **弹性计划**：不按固定日历排课，按「**每周建议练习包**」生成——
  - **基本功训练**（爬格子、开放和弦转换、节奏/扫弦练习）+ **歌曲段落练习** 穿插
  - 每项给出：练什么、练多久、目标标准
  - 时间不固定时给出「10 分钟版 / 30 分钟版 / 60 分钟版」三档练习包
- **动态调整（规则示例）**：本周练习 ≤ 2 天 → 下周建议量减半；连续 3 周达标 → 建议量 +10%；某首歌曲段落全部标记完成 → 推荐下一首同套路歌曲
- **进度分析**：周报/月报——练习时长趋势、歌曲掌握情况、**基本功清单未达标项**（如「和弦转换」未达标），并给出针对性练习（基于清单，不做模糊的 AI 判断）

### 3.4 【P1】课程进度跟踪

- 管理用户本地购买的**成田电吉他课程**（中级进阶课 81 课、核心技巧课 40 课）：建立课程目录（课名 + 本地文件路径）
- 标记学到哪一课、每课练习完成情况
- 与学习计划联动：计划中可引用课程内容；课程记录纳入打卡统计
- 注意：课程定位为进阶内容，**零基础阶段以基本功 + 入门歌曲为主**，课程作为进阶衔接

### 3.5 【P1】练习工具（全部确认需要）

1. **调音器**：手机麦克风收音调弦，支持标准调弦（EADGBE）与降半音等常见变调
2. **节拍器**：基础节拍器 + **与歌曲练习建议联动**（一键设为目标速度）
3. **和弦图 / 指板图**：常用和弦图库、指板音阶图（从五声音阶入门）
4. **录音回听**：录下练习片段，按时间线对比进步；存储策略见 §5

### 3.6 【P1】AI 答疑助手

- **场景**：练琴中遇到问题（和弦按不响、音色不对、谱看不懂、音箱不会调）
- **形式**：对话式，支持文字描述 + **上传照片**（按弦手型、音箱面板、谱子）
- **知识范围**：电吉他基础、设备使用（绑定用户设备档案）、歌曲练习

### 3.7 【P2】激励与统计

- 成就徽章/等级（多邻国风格：连续打卡里程碑、歌曲完成、累计时长）
- 统计报表与月度热力图

### 3.8 关键用户流程与验收标准（v0.2 新增）

**流程 1：拿到一首新歌想练（核心流程）**
1. 打开应用 → 按歌名搜索；未命中 → 上传音频文件
2. 等待分析（显示进度；分析是异步的，可离开页面）
3. 查看结果页：音色设置建议（含面板示意图）+ 和弦调性 + 练习步骤 + 难度评级
4. 点「开始练这首歌」→ 进入该歌练习页（节拍器默认设为建议速度）

验收标准：歌名命中缓存/曲库 ≤ 3 秒出结果；新音频分析 ≤ 5 分钟；分析失败时提供「手动选套路」兜底路径。

**流程 2：日常练琴打卡**
1. 手机打开应用 → 首页点「开始练习」→ 计时开始
2. 练完点结束 → 勾选练了什么（歌曲/基本功/课程）+ 备注
3. 打卡完成，更新连续天数

验收标准：从打开到开始计时 ≤ 3 次点击；离线可计时，联网后自动补传记录。

**流程 3：查看学习计划**
1. 打开「计划」页 → 本周练习包（三档时长可选）
2. 每项显示：练什么 / 练多久 / 目标标准；点击进入对应工具（节拍器/和弦图）

验收标准：计划生成规则透明（用户能看到「为什么这么建议」）。

**流程 4：遇到问题问 AI**
1. 练习页点「问一下」→ 文字或拍照提问 → 得到回答（自动带上设备档案上下文）

验收标准：回答引用当前设备档案；无网时明确提示不可用。

---

## 4. 设备参数参考（开发用）

> 以下参数来自网络检索，开发「音色设置建议」前请以实物面板/说明书核对为准。

### 4.1 Ibanez GRX40-LGY（复古白）

| 项目 | 参数 |
|---|---|
| 琴体/琴颈 | 椴木（Poplar）琴体；GRX 枫木琴颈（栓接） |
| 指板 | Jatoba，22 品，648mm 有效弦长 |
| 拾音器 | **HSS**：琴桥 Infinity R 双线圈 + 琴颈/中间 Infinity RS 单线圈（无源） |
| 档位开关 | **5 档**：1 琴颈单 / 2 琴颈+中间 / 3 中间单 / 4 中间+琴桥（并联）/ 5 琴桥双线圈 |
| 旋钮 | **1 音量 + 1 音色** |
| 琴桥 | T106 六点式摇把 |
| 出厂弦规格 | .009 ~ .042 |

**音色建议映射要点**：
- 清音/合唱类 → 琴颈或中间档（档位 1~3）
- 失真节奏 Riff → 琴桥双线圈（档位 5）
- 音色旋钮开大偏亮、关小偏闷；音量旋钮关小可模拟「清音化」

参考来源：[Ibanez 官网](https://www.ibanez.com/asia/products/detail/grx40_5y_02.html)、[Musik Produktiv](https://www.musik-produktiv.com/tr/ibanez-gio-grx40-bkn.html)

### 4.2 JOYO Jam Buddy 2

| 项目 | 参数 |
|---|---|
| 类型 | 便携数字建模音箱（桌面式，带脚钉） |
| 功率/喇叭 | 2×5W，双 2 英寸全频喇叭 + 被动低音辐射器 |
| 通道 | **3 通道**：Clean（清音）/ Rhythm（节奏失真）/ Lead（主音失真），3 组可存储预设 |
| 箱头模拟 | 14 种（Clean / Blues / Funk / Rock / Metal 等） |
| EQ | 3 段：Bass / Mid / Treble |
| 效果 | **MOD**（Flanger/Phaser/Tremolo/Chorus/Vibrato）、**Delay**（数字/模拟）、**Reverb**（Hall/Church），最多同时开 3 个 |
| 其他功能 | 30 秒 Looper、36 种节奏鼓机、内置调音器、蓝牙伴奏输入、USB-C（OTG 录音/充电） |
| 供电 | 内置电池（约 5 小时续航） |

**面板旋钮（含多功能按压）**：Guitar Vol、Music Vol、Bass/Save、Mid/Tune、Treble/D.Control、Gain/Quit、Reverb/D.Vol、Delay/D.Type、Mod/D.Speed、Amp/Ch.Volume

**脚钉**：Drive Mode（Rhythm/Lead 切换）、Channel（Drive/Clean 切换）、Looper 控制

**音色建议映射要点（示例）**：
- J-Rock 动漫歌清音扫弦 → Clean 通道 + Clean 箱模 + 少量 Chorus + 少量 Reverb
- 轻音《ふわふわ時間》节奏 → Rhythm 通道 + Rock 箱模 + 中等 Gain
- Beyond《光辉岁月》solo → Lead 通道 + Rock 箱模 + Delay
- 练琴时段优先用鼓机代替节拍器 App（二选一即可）

### 4.3 通用说明

- 音色建议引擎 = **歌曲归类结果** → **套路模板参数** → **设备能力模板** 的参数映射
- 设备模板数据需人工整理（首批 GRX40、Jam Buddy 2），新增设备时扩展模板库
- 设备面板示意图需按真实面板绘制（开发时提供实物照片）

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| 跨端同步 | PC / 手机数据实时一致，任何一端修改立即同步 |
| 离线可用 | 练琴环境可能无 WiFi：**调音器、节拍器、计时器、已缓存的设置建议离线可用**；打卡记录本地排队、联网自动补传。**不做全量离线同步**（v0.2 收窄范围，避免同步冲突复杂度） |
| 账号与登录 | 单用户；账号仅作多端同步凭据，简化为单账号登录，**不做多用户系统**（v0.2 修订） |
| 存储策略 | **已确认（v0.2.1）**：练琴录音默认存本地；上传的歌曲音频仅用于当次分析、不长期留云端；云端只存分析结果与打卡等结构化数据，避免超出免费额度 |
| 隐私 | 单人使用，数据仅自己可见 |
| 版权合规 | 上传音频仅用于个人分析不公开；曲谱数据自建，不提供公开曲谱库功能 |
| 可维护性 | 面向 AI 辅助开发：前后端分离、架构简单、文档齐全，避免过度设计 |
| 性能与体验 | 网页首屏加载快；AI 分析需显示进度、支持失败重试与「仅供参考」提示 |
| 提示语 | 分析结果（尤其 AI 识别）标注置信度与「仅供练习参考」 |

---

## 6. 成本评估（应「帮我评估」要求）

| 项目 | 尽量免费方案 | 说明 |
|---|---|---|
| 前端托管 | Cloudflare Pages / Vercel 免费档 | 0 元 |
| 数据库 + 登录 | Supabase 免费档（约 500MB 存储） | 0 元起步，个人使用很难超量 |
| AI 文本能力（练习建议/答疑/歌曲元信息） | 国内大模型免费额度或按量低价（如豆包、通义等） | 约 0~几元/月 |
| 音频 AI 分析（BPM/调性/和弦） | 开源链路：librosa（节奏/调性）等，免费但**准确率有限，需 M0 spike 实测**；如不达标再评估付费 API（每首歌约几分~几毛） | 0 元起步，付费上限每月几十元 |
| 曲谱数据 | **不依赖第三方曲谱 API**；种子曲库人工录入（v0.2 修订） | 0 元 |
| 微信推送 | 推送加 / Server酱免费档 | 0 元 |
| 域名 | 可选 | 约几十元/年 |

**结论**：尽量免费方案**可行**，月成本 ≈ 0~几元；**是否引入付费音频分析 API 由 M0 实测结果决定**，上限控制在每月几十元内。

---

## 7. 开发路线图建议

| 里程碑 | 内容 | 价值 |
|---|---|---|
| **M0（✅ 完成）** | **可行性验证 spike 已完成**。5 首真实歌曲对拍结论（v0.2.3）：①**BPM 可用**——「半速加倍」修正后误差 ≤1.5%（《星座になれたら》123.0 与真实 123 完全一致；局限：真实 <100 BPM 的慢歌会被误判，需后续用节拍强度比较法处理）；②**调性参考可用**——4/5 命中，唯一失误《NO, Thank You!》被前奏 B 大三和弦拉偏（聚合 chroma 在强力弦摇滚上有固有歧义）；③**套路归类初步可用**——4/5 合理，但样本集中于失真摇滚，清音类需继续校准；④**和弦识别薄弱**——每首仅 3~9 段、漏约一半和弦，需种子曲库数据或付费和弦识别 API。**结论：产品三层数据策略不变（种子库优先 → AI 参考+置信度 → 人工纠错）；M1 无技术依赖，可以开工；付费 API 决策顺延至 M2 前** | 验证核心卖点是否成立，避免盲目开发 |
| **M1（✅ 开发完成，2026-08-15）** | 设备档案 + 音色套路模板库（内置两套设备）+ 节拍器 + 调音器 + 练琴计时打卡 + 提醒。**实现要点**：app/ 目录 Vue3 + Vite + PWA；数据本地存储（存储抽象层，为后续 Supabase 云同步预留）；提醒为应用内提醒（系统推送/微信推送后续接入） | 先解决「会用设备」和「坚持练琴」 |
| **M2** | 歌曲分析 v1：种子曲库 + 歌名搜索 + 上传音频分析 → 套路归类/和弦/难度/设置建议 | 核心功能闭环 |
| **M3** | 学习计划（规则引擎练习包）+ 课程进度跟踪 + 统计报表 | 个性化指导 |
| **M4** | AI 答疑 + 桌面小组件（Capacitor 安卓壳）+ 录音回听 + M0 结论落地（付费 API 与否） | 完整版 |

每个里程碑可独立验收，用户可随时提出调整。

---

## 8. 待确认 / 风险问题

1. ✅ 已确认（2026-08-15）：接受 Capacitor 打包安卓壳，M4 阶段实施（方案与呈现形式见 2.2）
2. ✅ 已确认（2026-08-15）：种子曲库自建路线；后续仅需共同确认首批 20~50 首歌的歌单
3. ✅ 已确认（2026-08-15）：使用「推送加」（对比见 2.3）
4. ✅ 已确认（2026-08-15）：录音本地存储、云端仅存分析结果
5. **M0 对拍结论（v0.2.3）**：免费工具链可承担 BPM（±1.5%）、调性参考（4/5 命中）、套路归类（4/5 合理）；和弦识别薄弱，需种子曲库或付费 API；M1 可开工
6. **放弃第三方曲谱依赖**：确认接受「数据自建」路线（不依赖曲谱网站）
7. **音频格式约束**：酷狗 kgg 等加密格式无法分析，需以通用格式获取音频或走歌名检索（见 3.1.1）——已明确

---

## 附录 A：种子歌曲清单（示例，待确认）

- **轻音少女（K-ON!）**：ふわふわ時間、Don't say "lazy"、Cagayake!GIRLS
- **孤独摇滚（ぼっち・ざ・ろっく！）**：ギターと孤独と蒼い惑星、青春コンプレックス、あのバンド
- **MyGO!!!!!**：迷星叫、壱雫空
- **Ave Mujica**：黒のバースデイ 等
- **哭泣少女乐队（GBC）**：雑踏、僕らの街、空の箱
- **Beyond**：光辉岁月、海阔天空、真的爱你

## 附录 B：术语说明

- **PWA**：渐进式网页应用，网页可安装到手机桌面、离线缓存、系统通知
- **HSS**：琴桥双线圈 + 琴颈/中间单线圈的拾音器配置
- **P0/P1/P2**：功能优先级，P0 首版必须，P1 第二阶段，P2 后续增强
- **spike**：可行性验证实验，用小成本试跑一段关键链路，用真实数据回答「能不能做、能做到什么程度」
