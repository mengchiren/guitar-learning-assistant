# spike 虚拟环境依赖锁文件 —— 由本机 spike/.venv（Python 3.13.13）pip freeze 生成
# 用途：新电脑重建 spike 环境（见根目录《转接手册.md》第 4 节；自检脚本 env-check.sh）
# 安装：spike/.venv/Scripts/python.exe -m pip install -r spike/requirements.txt
#
# 注意事项：
# - 必须用 Python 3.13.x（本锁文件在该版本上生成并验证）。Python 3.13 移除了
#   stdlib 的 audioop/aifc/chunk/sunau，由 audioop-lts / standard-aifc /
#   standard-chunk / standard-sunau 四个兼容包补齐（librosa 依赖）。
# - ffmpeg 无需系统安装：imageio-ffmpeg 自带 ffmpeg 二进制（Windows 下为
#   ffmpeg-win-x86_64-v7.1.exe），用
#   spike/.venv/Scripts/python.exe -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"
#   取路径。
# - scikit-learn 当前没有任何 spike 脚本 import（历史遗留），保留仅为了
#   「本机 venv 完全复现」；如想精简可删去。
#
audioop-lts==0.2.2
audioread==3.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.0
decorator==5.3.1
idna==3.18
imageio-ffmpeg==0.6.0
joblib==1.5.3
lazy-loader==0.5
librosa==1.0.0
llvmlite==0.49.0
msgpack==1.2.1
narwhals==2.24.0
numba==0.67.0
numpy==2.5.2
packaging==26.3
pillow==12.3.0
platformdirs==4.11.2
pooch==1.9.0
pycparser==3.0
pymupdf==1.28.2
requests==2.34.2
scikit-learn==1.9.0
scipy==1.18.0
soundfile==0.14.0
soxr==1.1.0
standard-aifc==3.13.0
standard-chunk==3.13.0
standard-sunau==3.13.0
threadpoolctl==3.6.0
typing_extensions==4.16.0
urllib3==2.7.0
