# 练琴搭子 · 手机验收指南

> 给用户本人看的操作手册。目标：把线上版从头到尾点一遍，确认每个功能符合预期，有问题按第 4 节模板反馈。
> 站点地址：**https://guitar-learning-assistant.pages.dev**

## 0. 验收前准备

- 一部安卓手机（或 iPhone，安卓体验更完整），电脑浏览器（Chrome / Edge）也打开同一个网址备用。
- 手机能正常上网。国内网络访问这个站点偶尔会抽风，**打不开就多刷新几次，或换 Wi-Fi / 流量**。
- 如果以前在手机上打开过这个站（旧版本），新版部署后浏览器可能还在显示缓存旧界面。发现界面和本指南对不上时：**等一两分钟、刷新两三次**；还不行就在浏览器设置里清除这个站点的缓存数据（或先删掉旧的桌面图标）再打开。
- 验收大概需要 **20～30 分钟**。可以分几次做，每次验收完在下面清单里打勾。

## 1. 第一步：打开网站 + 装到手机桌面（PWA）

**操作**

1. 手机浏览器（推荐 Chrome 或系统自带浏览器）打开 `https://guitar-learning-assistant.pages.dev`。
2. 等页面完整加载，看到米白底、顶部写着「**练琴搭子**」、底部有「首页 / 工具 / 歌曲 / 我的」四个标签。
3. 把它装到桌面：
   - **安卓 Chrome**：点右上角「⋮」菜单 → 找「安装应用」或「添加到主屏幕」→ 确认。桌面出现「练琴搭子」图标。
   - **安卓其他浏览器**：菜单里找「添加到主屏幕 / 添加到桌面」。
   - **iPhone Safari**：点底部「分享」按钮 → 「添加到主屏幕」。
4. 点桌面图标打开，确认能像 App 一样全屏打开（没有浏览器地址栏）。

**通过标准**

- [ ] 网址能打开，页面标题/顶部显示「练琴搭子」
- [ ] 桌面图标安装成功，点图标能打开，界面正常不白屏
- [ ] 从图标打开后页面内容齐全（不是空白页）

> 电脑 Chrome 也可以装（地址栏右侧会出现一个「安装」小图标），但不是本次重点。

## 2. 手机验收清单（按这个顺序点一遍）

### A. 首页仪表盘

**操作**：打开应用停在首页（底部第一个标签「首页」）。

**通过标准**

- [ ] 大标题「今天练琴了吗？」
- [ ] 显示「今日累计练习」数字（第一次用是 0 分钟）
- [ ] 显示「连续打卡」天数（0 天）
- [ ] 有「开始练习」按钮
- [ ] 「最近 7 天」柱状图正常显示
- [ ] 「今日练习包（弹性）」列出 10 / 30 / 60 分钟三档建议

### B. 歌曲库（重点：种子歌数据）

**操作**：点底部「歌曲」标签。

**通过标准**

- [ ] 共 7 首种子歌，标题、歌手、BPM、调性都能看到，每张卡片带「种子」标签
- [ ] 7 首应为：NO, Thank You!（181）、Singing!（180）、空の箱（148）、KiLLKiSS（200）、雑踏、僕らの街（171）、星座になれたら（123）、**春日影（97，B 大调，清音伴奏）**
- [ ] 搜索框输入「春」→ 只剩春日影；清空 → 恢复全部
- [ ] 输入一个不存在的词（如「zzz」）→ 出现「没有找到」提示

### C. 歌曲详情 + 设备设置建议（重点：新手模式大白话）

**操作**：点进「春日影」。

**通过标准**

- [ ] 信息区显示：BPM 97、调性 B 大调、套路「清音伴奏」、标签「种子库 · M0 校准」
- [ ] 「设备设置建议」卡里是**大白话分步操作**：顶部 4 个小参数芯片，中间带编号的操作步骤（第 1 步、第 2 步…），底部「其余参数（新手可先不动）」清单
- [ ] 底部有按钮「用此 BPM 开节拍器（97 BPM）」
- [ ] 点它 → 跳到节拍器页，速度已经是 97
- [ ] 返回歌曲库，点「NO, Thank You!」→ 套路显示「失真节奏 Riff」，设备建议内容与春日影不同

### D. 节拍器（重点：声音 + 联动）

**操作**：点底部「工具」→「节拍器」。

**通过标准**

- [ ] 点「开始」→ 能听到规律的「嗒嗒」声，每拍 4 个圆点跳动，第一拍是红点（重音）
- [ ] 调 BPM 到 60 → 声音明显变慢；调到 180 → 明显变快
- [ ] 「打拍定速」：按自己舒服的节奏连点 6～8 下 → 显示速度接近你点按的速度
- [ ] 拍号选 3/4 → 每 3 拍一个重音；切回 4/4
- [ ] 点「停止」→ 声音停止

> 没声音先检查手机是否静音、媒体音量是否打开（见第 5 节常见问题）。

### E. 调音器（重点：麦克风权限）

**操作**：点「工具」→「调音器」。

**通过标准**

- [ ] 点「开始收音」→ 浏览器弹出「是否允许使用麦克风」→ 点允许
- [ ] 对着手机说话或轻弹吉他任意弦 → 屏幕上的音符名、指针会动
- [ ] 弹 6 弦（最粗的 E 弦）→ 指针应停在中间附近（琴已调准的话），偏差读数接近 0；故意把弦拧松一点再弹 → 指针偏左、偏差显示负数
- [ ] 点「停止收音」→ 停止
- [ ] 「没有麦克风？播放参考音」里点「6 弦 E2」→ 听到一声「叮——」；6 个按钮都能出声
- [ ] 点左上角返回键能回工具页

> 注意：手机上**必须用本指南的线上网址**（https 开头）才能弹麦克风权限。之前局域网 IP 的 http 地址不行，别用。

### F. 练琴打卡计时（重点：跨页计时 + 打卡记录）

**操作**：点「开始练习」（首页大按钮，或底部标签进「首页」→「开始练习」）。

**通过标准**

- [ ] 进入「练习计时」页，大数字计时器从 00:00 开始走
- [ ] 点「开始」→ 计时走动；**切到「歌曲」或「首页」标签** → 底部/页面上出现悬浮胶囊「练习中 00:xx」且数字继续走（这就是「切页不停表」）
- [ ] 点悬浮胶囊 → 跳回练习计时页，时间没丢
- [ ] 等一两分钟 → 点「结束打卡」→ 出现「这次练了什么？」→ 选「基本功」标签、备注写点字 → 「保存打卡」
- [ ] 显示「打卡成功」，包含「今日累计 X 分钟，连续 Y 天」
- [ ] 回首页 → 「今日累计练习」不再是 0，连续打卡变成 1 天，「最近 7 天」最后一根柱子（今天）变红

**补卡**（顺手验）：

- [ ] 练习计时页下方「补卡」→ 日期选昨天、时长填 15 → 点「补卡」→ 显示「已补卡」→ 首页「最近 7 天」昨天也出现柱子

### G. 练琴提醒

**操作**：底部「我的」→「提醒设置」。

**通过标准**

- [ ] 打开「每日练琴提醒」开关 → 出现时间选择框
- [ ] 时间改成**当前时间之前**（比如现在下午 3 点，设成 14:00），并且今天还没练琴 → 回首页 → 顶部出现红色竖线卡片「到点啦，今天还没练琴！」
- [ ] 把开关关掉 → 回首页提示消失（没练过琴时关掉开关就没有横幅）
- [ ] 点「请求系统通知权限」→ 浏览器弹授权框（允许或拒绝都行，当前版本只是提前问一下，真正的系统推送在后续版本）

> 注：当前版本（M1）提醒只是「打开应用时检查」，不会像微信那样后台弹通知，这是预期行为，不是 bug。

### H. 添加歌曲：上传分析（重点）

**操作**：底部「歌曲」→「添加歌曲」，保持「上传音频分析」标签。

**通过标准（有 mp3/flac 的话）**

- [ ] 点「选择音频文件」→ 从手机里选一首 mp3（或 flac）→ 歌名自动填上文件名
- [ ] 点「开始分析」→ 显示「正在分析…」→ 几秒到几十秒后出「分析结果预览」：BPM、调性（带高/中/低置信度标签）、套路、候选调性、分析备注
- [ ] 点「保存到我的歌单」→ 跳到这首歌详情页，标签显示「自动分析」
- [ ] 返回歌曲库 → 这首歌出现，带「我的」标签，列表里显示分析出的 BPM

**通过标准（kgg 拒绝）**

- [ ] 如果手机里有酷狗下载的 .kgg 文件，选它 → 点「开始分析」→ 立刻提示「这是加密格式（酷狗 kgg 等），无法分析」（不会卡住、不会闪退）

**人工纠错（重点）**

- [ ] 打开刚才自动分析的那首歌详情页 → 底部「人工纠错」区把 BPM 改成 120、保存 → 显示「已保存，展示值已更新」→ 歌曲信息区 BPM 变成 120，列表里也跟着变

**删除**：

- [ ] 详情页右上角「删除」→ 歌曲从列表消失（种子歌没有删除按钮，是正常的）

### I. 添加歌曲：手动录入（兜底）

**操作**：「添加歌曲」→ 切到「手动录入」标签。

**通过标准**

- [ ] 歌名留空直接「保存」→ 提示「歌名不能为空」
- [ ] 歌名随便填（如「测试歌」）、BPM 填 100、调性选「C 大调」、套路选「轻过载节奏」→「保存」→ 显示「已保存」并跳到详情页
- [ ] 详情页显示手动填的 BPM / 调性 / 套路，标签「手动录入」
- [ ] 到歌曲库把「测试歌」删掉（验证完不留垃圾数据）

### J. 我的页 + 新手模式开关（重点）

**操作**：底部「我的」。

**通过标准**

- [ ] 三个入口：我的设备（显示 Ibanez GRX40 + JOYO Jam Buddy 2）、提醒设置、音色套路库（5 套常用音色设置）
- [ ] 「新手模式（设备建议）」开关**默认是开的**
- [ ] 进「音色套路库」→ 5 套全部是「参数芯片 + 大白话步骤」形式
- [ ] 回「我的」关掉新手模式 → 再进套路库 → 变成「全部参数表」形式（吉他档位/音色旋钮/通道/箱模/Gain/EQ/MOD/Delay/Reverb 九宫格）
- [ ] **再打开新手模式**（保持你平时用大白话模式）
- [ ] 「我的设备」页：点吉他卡片/音箱卡片可以选中（边框变红、「使用中」标签），设备参数展示正常

### K. 数据保存在本机

**操作**：打卡一条、加一首歌之后，把应用整个关掉（从后台划掉）重新打开。

**通过标准**

- [ ] 打卡记录、连续天数还在
- [ ] 添加的歌还在
- [ ] 新手模式开关、提醒设置保持你上次的设置

### L. 录音回听 + 自动对拍（v0.4.0 新增）

**操作**：练习页「练琴录音」卡片（或工具页「录音回听」）→ 点「开始录音」→ 弹琴/唱/说话几秒 → 点「停止」。

**通过标准**

- [ ] 第一次点会弹麦克风权限框，允许后出现「正在录音」计时
- [ ] 停止后几秒内显示「测得约 xx BPM（置信度 x）」（清音弹奏测不准是正常的，可手动改）
- [ ] 选分类（选「歌曲」时能关联到种子歌）→ 保存 → 提示已保存
- [ ] 历史录音列表出现这条（日期/时长/分类/歌曲/BPM/备注）→ 点「播放」能听到刚才录的声音
- [ ] 点「删除」→ 再点「确认删除？」→ 列表清空
- [ ] 录一条后把应用整个关掉重开 → 录音还在（存本机数据库）

### M. AI 答疑（v0.4.1 新增）

**第一步：配 API Key（只配一次，在电脑上做）**

1. 注册 DeepSeek（platform.deepseek.com）→ 充值几块钱 → 控制台「API keys」生成一个 `sk-` 开头的 Key，复制备用。
2. 打开 Cloudflare 控制台 → Pages → guitar-learning-assistant 项目 → Settings → Environment variables → 添加变量：名字 `AI_KEY_DEEPSEEK`，值粘贴你的 Key → 保存。
3. 回到项目 Deployments → 最新一次部署点「⋯」→ **Retry deployment（重新部署）**，等它变绿。

**第一步半：配访问令牌（v0.5.0 新增，防盗刷，只配一次）**

> 为什么需要：API Key 藏在服务器里，但任何人都能调那个接口白嫖你的额度。访问令牌 = 只有你知道的暗号，应用每次请求都要出示它。

1. Cloudflare 控制台 → Pages → guitar-learning-assistant → Settings → Environment variables → 添加变量：名字 **`ASK_TOKEN`**，值填一长串随机字符（复制我给你的令牌，或自己随便打 20 位以上字母数字）。
2. 保存后 **Retry deployment（重新部署）** 等它变绿。
3. 手机/电脑打开应用 → 工具页 →「AI 答疑」→ 页面顶部出现「AI 访问令牌」输入框 → 把同一个令牌粘贴进去 → 保存。
4. 之后每次换新设备打开 AI 答疑，第一次都要填一次这个令牌（和登录密码一个道理）。

**第二步：手机验收**

**操作**：工具页 → 「AI 答疑」卡片 → 输入「F 和弦按不响怎么办？」→ 发送。

**通过标准**

- [ ] 没填令牌时点发送 → 提示「请先填写 AI 访问令牌」，不报错崩溃
- [ ] 填错令牌（随便乱输）→ 提示「访问令牌不对」
- [ ] 填对令牌后，回复是中文、大白话、分步骤的练琴建议
- [ ] 顶部能切换模型（DeepSeek / 豆包 / 通义千问），切换后继续问也能答
- [ ] 没配 Key 的平台会提示「还没配置 API Key」，而不是报错崩溃
- [ ] 歌曲详情页点「问 AI 这首歌怎么练」→ 聊天页显示「已带上刚才页面的信息」→ 提问时 AI 知道你说的是哪首歌
- [ ] 对话关掉应用重开还在（存本机）；「清空对话」能清掉

### N. 数据备份 / 恢复（v0.5.0 新增）

**操作**：底部「我的」→ 找到「数据备份」卡片。

**通过标准**

- [ ] 点「导出备份文件」→ 浏览器下载一个 `练琴搭子备份-日期.json` 文件
- [ ] 把文件保存好（可以存到微信文件传输助手 / 网盘）
- [ ] 模拟数据丢失：在设置里清掉这个网站的浏览器数据（或者换一台手机/浏览器打开同一网址）
- [ ] 「我的」→「数据备份」→「从备份恢复」→ 选刚才那个文件 → 弹窗确认 → 页面自动刷新
- [ ] 刷新后打卡记录、歌单、曲谱、设置都回来了（录音不包含在备份里，是预期的）

## 3. 电脑浏览器验收（顺手过一遍）

**操作**：电脑 Chrome / Edge 打开同一个网址，窗口宽度正常（≥768px）。

**通过标准**

- [ ] 顶部是**横条导航**（练琴搭子 logo + 首页/工具/歌曲/我的 + 右侧节拍器、调音器快捷图标），没有底部标签栏
- [ ] 首页是**多栏布局**（左边今日练习，右边连续打卡/练习包/提醒）
- [ ] 歌曲库是**两列**卡片
- [ ] 开始练习计时 → 切到其他页 → 顶栏出现「练习中 00:xx」胶囊，点击能跳回练习页
- [ ] 把窗口拖窄到手机宽度 → 布局自动变回手机样式（底部标签栏出现）

## 4. 验收完怎么反馈

逐项打勾。有问题的项，按这个模板发给我（一条问题一段）：

```
功能：调音器
设备：小米 14，系统浏览器
步骤：工具 → 调音器 → 开始收音
实际：没弹权限框，音符一直显示「--」
期望：弹权限框，弹琴时指针动
```

尽量附**截图或录屏**。不会截图没关系，文字描述清楚就行。以下信息很重要：**手机型号 + 用的哪个浏览器**、出问题的页面、操作到哪一步、看到什么提示。

## 5. 常见问题（先自己对一下）

| 现象 | 原因与处理 |
|---|---|
| 网址打不开 / 一直转圈 | 国内访问 pages.dev 会间歇性抽风。多刷新几次、换 Wi-Fi 和流量轮流试。 |
| 打开看到的是旧界面 | 等新版本部署生效（约 1 分钟）+ 多刷新两次；不行就清浏览器缓存或删掉旧桌面图标重装。 |
| 节拍器「开始」没声音 | 手机媒体音量/静音开关；iPhone 第一次需要先点一下屏幕任意处再开始。 |
| 调音器点「开始收音」没弹权限 | 确认网址是 https 开头的线上地址；浏览器设置里给这个站点开了麦克风权限；之前拒绝过的话要在浏览器「网站设置」里手动允许。 |
| 调音器指针乱跳/不动 | 环境太吵会干扰收音；离手机近一点、安静一点再试。 |
| 上传分析转很久不出结果 | 手机分析一首歌要几秒到几十秒是正常的（文件大就慢）。超 100MB 会直接提示。 |
| 上传后提示「无法解码」 | 换个格式（mp3 优先）或换源。 |
| 选 kgg 文件提示加密 | 预期行为。换 mp3/flac 版本，或用「手动录入」。 |
| 提醒时间到了没通知 | M1 版只是打开应用时检查，不会后台弹通知。这是预期行为，后续版本接系统推送。 |
| 录音点「开始录音」没反应/提示不支持 | 确认网址是 https 开头的线上地址；浏览器设置里允许麦克风；安卓 Chrome 和 iPhone Safari 都支持，其他浏览器可能不行。 |
| 录音测出来的 BPM 明显不对 | 清音弹奏/环境噪音会让对拍不准，这是预期行为；BPM 框里手动改一下，保存即可。 |
| AI 答疑提示「还没配置 API Key」 | 去 Cloudflare 控制台加 `AI_KEY_DEEPSEEK` 环境变量后**重新部署一次**；换一个平台问也一样要配对应 Key。 |
| AI 答疑提示「访问令牌」相关错误 | 服务端还没配 `ASK_TOKEN` 环境变量（要去 Cloudflare 后台加 + 重新部署），或应用里填的令牌和后台不一致——重新粘贴一次。 |
| AI 回答慢或失败 | 网络到模型服务商的延迟；稍等重试，或换个模型试。 |
| 手机端页面布局乱/字太小 | 浏览器版本太旧可能出现；更新浏览器或换 Chrome 试试。 |
